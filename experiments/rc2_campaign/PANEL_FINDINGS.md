# RC2 Campaign prereg — ultracode panel findings

Panel: 4 adversarial lenses (methodology / degeneracy-Goodhart / ground-truth-slate / repo-facts)
→ dedup → 2 independent refuters per finding (finding survives only if BOTH fail to refute).
57 agents, 61 min. 53 raw findings → 15 confirmed, 11 refuted.

Confirmed findings are GRAFT OBLIGATIONS for the draft before lock.

## Confirmed

### C1. [BLOCKER] Registered seed streams 17M/34M/51M/68M are NOT fresh — they are exactly Phase C R2's recorded streams

**Section:** §2 seed streams

**Claim:** [2 lenses converged: methodology + repofacts, independently and identically] The draft registers 'Seed streams (fresh, never used by any recorded probe)' at 17M/34M/51M/68M, but PREREGISTRATION_R2.md pins base_seed 17 → 17M (Stage-0 gen) / 34M (arm-R gen) / 51M (mutation rng) / 68M (selection rng) / 85M (bootstrap) for the Phase C 2× replicate, whose complete genome sequence, family composition, archive trajectory, elimination outcome and top-10 identities are recorded on disk (replicate2x/probe_results.csv, RESULTS.md). The campaign would re-generate a genome stream the authors have already inspected: the freshness claim is false, the prereg is contaminated (e.g., whether ≥20-valid-per-family is reachable at these seeds is already knowable), and arm-comparison expectations are biased. The draft also omits the bootstrap stream entirely.

**Evidence:** PREREGISTRATION_DRAFT.md:27-30 ('fresh, never used by any recorded probe') vs experiments/rc2_archive/PREREGISTRATION_R2.md:9-10 ('base_seed = 17 → seed streams 17M (Stage-0 gen) / 34M (arm-R gen) / 51M (mutation rng) / 68M (selection rng) / 85M (bootstrap)'); R2 ran and is recorded in experiments/rc2_archive/RESULTS.md:93-128 and replicate2x/; R2's own disjointness assert (PREREGISTRATION_R2.md:11) was only against run-1's 13M-family, not against future reuse.

**Proposed fix:** Advance to a genuinely unused base by the established ×{1..5} rule (e.g., base 19 or 21 → 19M/38M/57M/76M/95M), register the bootstrap stream the draft omits, and add an R2-style hard disjointness assert listing ALL previously used streams (base-13 and base-17 families with attempt offsets, anchor streams 42-47, smoke offsets); the runner refuses to start otherwise.

<details><summary>Refuter 1 (failed to refute)</summary>

Finding verified end-to-end; refutation fails. (1) PREREGISTRATION_DRAFT.md:27-29 registers 17M/34M/51M/68M as 'fresh, never used by any recorded probe' with no disjointness assert anywhere in the document, and omits a bootstrap stream. (2) PREREGISTRATION_R2.md:9-10 pins base_seed 17 to exactly 17M/34M/51M/68M/85M with identical role assignments (Stage-0 gen / arm-R gen / mutation rng / selection rng), derivation confirmed in run_probe.py:240-244 (base x{1..5} x 1M). (3) R2 ran and is recorded on disk: replicate2x/probe_results.csv (234 rows), arm logs, checkpoint.json, run log, and RESULTS.md:93-138 (ARCHIVE_GO). The freshness claim is therefore false. (4) Materiality confirmed and even stronger than claimed: replicate2x/probe_results.md records Stage-0 family-validity counts at these seeds (territory 26 / elimination 0 / connection 48 / threshold 23 valid at 252 attempts), so the draft's own >=20-valid-per-family BAR W-PG condition is already knowable per family, and top-10 identities/family compositions from these streams were already inspected in the ARCHIVE_GO synthesis. The campaign uses Phase C verbatim generator/mutation configs, so the raw genome stream reproduces R2's; the new simultaneous-move quick-reject only filters it, it does not refresh it. (5) R2's disjointness coverage (PREREGISTRATION_R2.md:11-12 prose; run_probe.py:245-249 assert) only addresses run-1 base-13 streams, not future reuse — as claimed. (6) The proposed fix is sound and breaks nothing recorded: base 19 (19M/38M/57M/76M/95M) is disjoint from base-13 family (offsets <= +777k smoke), base-17 family (attempt offsets in the hundreds/thousands), and anchor streams 42-47; rc2_campaign/ contains only the draft, so no built runner is affected. Severity 'blocker' is correct for a prereg-lock review: the central independence claim of section 2 is false and locking it would contaminate the campaign and bias arm-comparison expectations. Only nit: the finding calls PREREGISTRATION_R2.md:11 an 'assert' when it is prose (the runtime assert is narrower, in run_probe.py), which does not affect substance.

</details>

<details><summary>Refuter 2 (failed to refute)</summary>

Refutation attempt FAILED on all axes; finding stands at blocker severity. (1) Exact evidence match: PREREGISTRATION_DRAFT.md:27-31 registers 17M/34M/51M/68M as 'fresh, never used by any recorded probe', while experiments/rc2_archive/PREREGISTRATION_R2.md:9-12 pins base_seed 17 to exactly those streams (plus 85M bootstrap) via the x{1..5} rule, confirmed in run_probe.py:240-244 with the transcribed --base-seed 17 invocation. (2) R2 verifiably ran: RESULTS.md:93-138 records the ARCHIVE_GO verdict with per-arm top-10 family compositions; replicate2x/ holds probe_results.csv, 601-line arm_R/arm_M logs with genome hashes and outcomes, and checkpoint.json — the genome stream at these seeds is on disk and was inspected. (3) Contamination is mechanistic, not hypothetical: the draft reuses GameGeneratorV2 DEFAULT 'Phase C verbatim', run_probe.py:274-276 documents seed-only determinism of generated content, and eval seeds are content-derived by the identical formula (R2 prereg itself notes identical batches for shared genomes) — so Stage-0/arm-R genome identities, families, and validity outcomes are pre-knowable, directly contaminating BAR W-PG's >=20-valid-per-family reachability and the arm-R set feeding BAR H-PG. The new simultaneous-move quick-reject is a deterministic subset filter and does not restore freshness. (4) Secondary claims verified: R2's disjointness assert covered only run-1's base-13 family; the draft omits the 5th (bootstrap) stream that the verbatim Phase C machinery uses (run_probe.py:244,602); no other draft section addresses disjointness. (5) The proposed fix breaks nothing recorded: anchor streams 42-47 are literal small seeds (ANCHOR_CALIBRATION.md 'streams [42,43]'), smoke offsets are +777k near the 13M family, and base 19 (19M/38M/57M/76M/95M) or 21 is disjoint from all recorded streams including attempt offsets. Severity: blocker is appropriate — a prereg whose central seed-freshness assertion is false cannot lock, since the project's own house style (R2 hard assert, registered-disjoint smoke, fresh anchor streams) treats stream freshness as load-bearing for bar validity. Only nit: the finding cites probe_results.csv for the 'complete genome sequence' where arm_M_log.csv/arm_R_log.csv/checkpoint.json are the fuller records — immaterial to the conclusion.

</details>

### C2. [BLOCKER] RUSH/TILT guards are defined on tactical rollouts the inherited pipeline never produces; constants validated only under TacticalAgent mirrored pairs

**Section:** §4 insertion guards

**Claim:** [All 4 lenses converged — strongest cross-lens signal in the review] The draft computes RUSH/TILT from 'descriptor-rollout records' and the design spec claims they come 'from the descriptor tactical rollouts the pipeline already runs'. This is false on disk: the Phase C genome eval inherited verbatim is metrics.rollout_traces.run_protocol (aggregated by metrics.descriptors.descriptor_row), which is hard-limited to RandomAgent and GreedyAgent (raises on anything else) with no seat mirroring (agent0 always player 1). TacticalAgent mirrored-pair rollouts exist only as probe-local code in rc2_descriptor_v2/run_probe.py. The RUSH=0.25 / TILT=0.80 constants were validated exclusively under TacticalAgent-vs-TacticalAgent 50 mirrored seed pairs (n=100), and both TILT positives sat EXACTLY at the 0.80 boundary; TILT's semantics require mirrored seeds run_protocol cannot supply. So either (a) the guards silently run on random/greedy traces where the constants were never validated — and Phase D documented random-play behavior diverging from competent play on precisely the degenerate genomes the guards exist to catch (S2's archive guard miss, S1's first-turn win invisible to random traces), letting S1-class games reach the slate and burn 3 of 21 verdicts each — or (b) a new TacticalAgent stage must be added, contradicting 'Phase C machinery verbatim' and absent from CAL-C's cost model (at validated n=100, ~4.8 min wall/genome → ~96 h over 1200 genomes; reduced n changes false-fire rates materially, e.g., TILT at 10 decisive games: P(≥0.80 | fair) = 5.5% → ~60+ spurious vetoes).

**Evidence:** PREREGISTRATION_DRAFT.md:51-54; 2026-06-12-rc2-campaign-design.md:48-52 ('from the descriptor tactical rollouts the pipeline already runs'); metrics/rollout_traces.py:47-57 (random/greedy only, raises otherwise; agent0 fixed to player 1), :91-120 (run_protocol = random + greedy halves, no mirroring); rc2_archive/PREREGISTRATION.md:21-23 (genome eval = run_protocol + descriptor_row); rc2_descriptor_v2/run_probe.py:70-76 (N_PAIRS=50, constants), :190-235 (TacticalAgent mirrored pairs, probe-local); rc2_descriptor_v2/RESULTS.md:5, :12-14 (S4/S5 TILT 'exactly 0.80'); rc2_descriptor_v2 RESULTS.md:4-5 (15 games × 100 rollouts = 72 min wall); evaluations/rc2_phase_d/RESULTS.md S2/S1 mechanism notes.

**Proposed fix:** Register an explicit TacticalAgent guard stage: lift rollout_tactical/TacticalAgent into metrics/, pinned n (e.g., 24 games = 12 mirrored pairs) with the run_probe.py mirrored-seed scheme and content-derived base seeds, run ONLY at insertion-offer time (genomes whose floored T1-PG beats the incumbent) so cost scales with offers not B; include per-genome guard cost in CAL-C timing and the timeout treatment. Add a pre-lock CAL-G re-validation at the chosen n: RUSH fires on S1, TILT fires on ≥1 of {S4,S5}, both silent on d4015/e1453/protected anchors — re-pricing the 0.80 constant for smaller n with binomial false-fire rates published. Alternative (must be chosen explicitly): re-validate 0.25/0.80 on the existing random+greedy records or the genome's own seat-balanced T1 UCT records against the descriptor-v2 anchor set before lock — they were never tested there.

<details><summary>Refuter 1 (failed to refute)</summary>

NOT REFUTED — the finding is real, evidence-verified, and correctly severitized as a blocker; only one secondary cost number is overstated.

Verified against disk:
(1) Draft §4 (experiments/rc2_campaign/PREREGISTRATION_DRAFT.md:51-54) does compute RUSH/TILT from "descriptor-rollout records"/"mirrored pairs; same source", while explicitly contrasting REACH-v3's source ("the genome's own T1 games... zero extra cost") — so the descriptor-record assignment is the author's deliberate intent, not a typo.
(2) The design-authority doc (docs/superpowers/specs/2026-06-12-rc2-campaign-design.md:48-50) claims these come "from the descriptor tactical rollouts the pipeline already runs" — false on disk: the inherited Phase C genome eval is metrics.rollout_traces.run_protocol + descriptor_row (rc2_archive/PREREGISTRATION.md:21-23; rc2_archive/run_probe.py:59,136), and rollout_traces.py:47-57 raises ValueError on anything but random/greedy, fixes agent0 to player 1, and run_protocol (lines 91-114) has no seat mirroring. The descriptor-v2 prereg makes mirroring constitutive of TILT ("mirroring removes seed luck", rc2_descriptor_v2/PREREGISTRATION.md:31-32).
(3) RUSH=0.25/TILT=0.80 were validated exclusively under TacticalAgent-vs-TacticalAgent 50 mirrored pairs (run_probe.py:70-76, harness probe-local at lines 201-237; the only other tactical harness is another experiment-local copy in reach_endcause/reach_v2.py; nothing in metrics/ or the archive runner runs tactical rollouts). Both TILT positives sat exactly at the 0.80 boundary (rc2_descriptor_v2/RESULTS.md line 14) — maximally brittle to any policy or n change.
(4) Phase D RESULTS.md documents random-trace divergence on exactly the guard targets: S1's winning line is never played by random rollouts; S2's degeneracy was invisible to the random-rollout validity guard. So resolution (a) — guards on random/greedy records — uses constants never validated there and plausibly misses the precise failure modes the guards exist for; ANCHOR_CALIBRATION.md:46 makes RUSH+TILT-on-insertion a BINDING input for any PG search spend, and slate entries burn 3 of 21 verdicts each.
(5) Resolution (b) — adding a TacticalAgent stage — contradicts recorded owner decision 4 ("Phase C archive machinery verbatim... no other machinery change") and is absent from the draft, so CAL-C as registered would not price it.
(6) Math: P(>=0.80 | fair, 10 decisive) = 56/1024 = 5.47% — reviewer's 5.5% correct. 72 min / 15 games = 4.8 min wall/genome at 7 workers; x1200 = 96 h — arithmetic correct.

One quantitative overstatement, not verdict-changing: the probe roster's cost is dominated (~88%) by the two field-connection games (s_flip_r2 20,060 s + a1 6,697 s serial per probe_results.csv), which are outside the campaign's generator space; generator-space-like genomes average ~280 s serial at n=100, giving ~13 h wall at 7 workers over 1200 genomes, not ~96 h. The corrected figure still exceeds the registered 8 h cap on its own, so the cost conclusion (material and unmodeled) survives. Likewise "~60+ spurious vetoes" assumes near-universal guard checks; offer-time-only checking gives fewer, but the false-fire-rate sensitivity at the exact-boundary 0.80 constant is sound.

The draft does not handle the concern elsewhere (the DRAFT marks section relies on guards working; no trace policy/n/seeds/cost are registered for guard computation), and the proposed fix breaks nothing recorded (new metrics module leaves locked files untouched; offer-time checking is consistent with "veto on archive entry"; the owner-decision amendment and constant re-pricing it requires are explicitly surfaced). For a document whose purpose is pre-data precision, an unimplementable-as-validated guard section resting on a false factual premise in the design doc, protecting the campaign's GO bars from the exact documented Goodhart failure, is a lock-blocking defect. Severity: blocker stands.

</details>

<details><summary>Refuter 2 (failed to refute)</summary>

NOT REFUTED — the finding survives adversarial re-verification; blocker severity is warranted.

Evidence verification (all citations check out on disk): (1) PREREGISTRATION_DRAFT.md:51-54 defines RUSH/TILT on "decisive tactical rollouts" while sourcing them to "(descriptor-rollout records)" and "(mirrored pairs; same source)" — internally contradictory. (2) metrics/rollout_traces.py is hard-limited to RandomAgent/GreedyAgent (raises ValueError on any other policy, lines 47-57) with agent0 always seat 1 and no mirroring (run_protocol, lines 91-114). (3) The inherited Phase C genome eval is exactly run_protocol + descriptor_row (rc2_archive/PREREGISTRATION.md:21-23; rc2_archive/run_probe.py:59,136 confirm in code). (4) The design spec's claim that guards come "from the descriptor tactical rollouts the pipeline already runs" (2026-06-12-rc2-campaign-design.md:48-49) is false on disk: no pipeline stage anywhere produces tactical rollouts; the mirrored-pair harness (pair_seeds/rollout_tactical) exists only in rc2_descriptor_v2/run_probe.py:193-237 (plus its test and reach_endcause copies). Only the TacticalAgent class itself lives in metrics/tactical_agent.py — a minor wording imprecision in the finding that does not change it. (5) RUSH=0.25/TILT=0.80 were validated exclusively under TacticalAgent 50 mirrored seed pairs n=100 (run_probe.py:70-76), and both TILT positives sat EXACTLY at 0.80 (RESULTS.md line 14; probe_results.csv tilt_p1_share=0.8 for S4 and S5) — boundary-fragile to any change of substrate or n. (6) Branch (a) materiality is independently documented in evaluations/rc2_phase_d/RESULTS.md: S1's random rollouts "never play the winning line" (vs tactical rush share 1.00, mean 4.02 plies) and S2 escaped the random-trace validity guard; Phase D's registered consequence #2 explicitly requires ">80/20 seat tilt under mirrored COMPETENT rollouts". Guards computed on random/greedy records are therefore plausibly silent exactly on the genome class they exist to veto, and S1-class forced first-turn wins are plausibly high-T1-PG (deep-but-not-shallow search finds the forced line), making the guards load-bearing under the new maximand; a dead guard lets such games reach the 7-game/3-team slate at 3 of 21 verdicts each. (7) Branch (b) contradicts owner decision #4 ("Phase C archive machinery verbatim ... no other machinery change") and registers no n, seeds, or cost for the new stage.

Recomputed numbers: TILT binomial P(>=8 of 10 | fair) = 56/1024 = 5.47% (reviewer's 5.5% correct; ~66 expected spurious vetoes over 1200 fair genomes at 10 decisive games is arithmetically right as an illustration). The reviewer's cost figure is the one soft spot: "4.8 min wall/genome -> 96 h" mislabels a 7-worker wall-per-game average that is 88% dominated by two 484-cell field_connection games impossible in the campaign space (max_total_cells=64). However, the corrected campaign-space estimate from the same CSV (280 s serial/genome at n=100 over the 13 <=64-cell roster games -> 93.5 h serial, ~13.4 h wall at 7 workers) still exceeds the registered 8 h cap on its own, before any UCT screening cost — so the materiality conclusion stands; coincidentally ~96 h is approximately correct as serial hours.

Refutation attempts that failed: (a) "TILT doesn't strictly need mirroring when both seats run the same policy" — true in expectation for random-vs-random, but irrelevant: the constants were never validated on those traces, and the validated substrate is precisely where the degenerate behaviors manifest. (b) "The draft handles it elsewhere" — no: the DRAFT-marks panel list omits the guard substrate; CAL-C is an empirical end-to-end timing gate with no itemized stages, so it only catches the cost if a tactical stage is actually built, and would then force re-scoping the owner-decided B=600 — a registration contradiction either way. (c) "The fix breaks something recorded" — no: it is a pre-lock amendment; TacticalAgent already lives in metrics/ (only the harness needs lifting), insertion-offer-time guarding is compatible with Phase C offer mechanics, and the required re-pricing of 0.80 at smaller n is exactly what boundary-sitting positives demand.

Severity: correct as blocker. As written the prereg cannot be locked: either the build silently computes guards on a substrate where the constants were never validated and are plausibly inert on the target failure class (defeating S-GO-1 and burning slate verdicts), or the builder must improvise an unregistered stage that contradicts the design's "verbatim" owner decision and the cost cap. Key files: /Users/jamesbrowne/aigame/experiments/rc2_campaign/PREREGISTRATION_DRAFT.md, /Users/jamesbrowne/aigame/docs/superpowers/specs/2026-06-12-rc2-campaign-design.md, /Users/jamesbrowne/aigame/metrics/rollout_traces.py, /Users/jamesbrowne/aigame/experiments/rc2_descriptor_v2/run_probe.py, /Users/jamesbrowne/aigame/experiments/rc2_descriptor_v2/probe_results.csv, /Users/jamesbrowne/aigame/evaluations/rc2_phase_d/RESULTS.md.

</details>

### C3. [BLOCKER] BAR W-PG quantifier flip ('fail in ANY family → ARCHIVE_KILL') would have killed Phase C's own validated replicate

**Section:** §6 BAR W-PG

**Claim:** [2 lenses converged: methodology (blocker) + degeneracy] The draft flips Phase C's validated grammar ('BAR W passes iff ≥ 2 sampled families are LIVE') to 'fail in ANY sampled family → ARCHIVE_KILL'. Applied to Phase C R2's own recorded data, the draft's wording fires ARCHIVE_KILL on a run that legitimately reached ARCHIVE_GO: threshold family spread 0.0560 was below the floor while territory/connection passed. Threshold liveness is documented as non-replicating ('marginal'), threshold's PG anchors are the weakest (S2 +0.042, e1453 −0.125 at T1), and threshold-family noise is the largest of any family (e1453 cross-stream swing 0.4167) — so a threshold family reaching 20 valid genomes makes spurious ARCHIVE_KILL the modal outcome, handing a whole-campaign kill to a family the signal never claimed to discriminate within, before any search, including in the families where PG's anchors actually live (S4/S5/d4015/S3).

**Evidence:** PREREGISTRATION_DRAFT.md:72-75 ('Fail in any sampled family → ARCHIVE_KILL') vs rc2_archive/PREREGISTRATION.md:77-78 ('BAR W passes iff ≥ 2 sampled families are LIVE'); rc2_archive/RESULTS.md:103 ('threshold DEAD at 0.0560 (was 0.0734 in run 1)'), :118-121 ('threshold's within-family liveness did NOT replicate'); cost_tiering.json:140-143 (e1453 T1 per-stream +0.0833 / −0.3333); cost_tiering.json T1 threshold anchors S2 +0.0417, e1453 −0.125.

**Proposed fix:** Restore the Phase C quantifier: BAR W passes iff ≥ 2 qualifying families are LIVE (per-family LIVE/DEAD reported; dead families excluded from slate-eligible cells); ARCHIVE_KILL only if < 2 LIVE. Alternatively scope the any-family-kill to families from which the slate may draw elites. If the owner wants the stricter all-families form, it must be re-derived with a registered per-family false-kill rate computed at the measured family-specific σ, not inherited 3σ semantics. Pin which at lock.

<details><summary>Refuter 1 (failed to refute)</summary>

NOT REFUTED. I attempted refutation on five fronts; the finding survived all of them, with the evidence verified line-by-line.

1. Quantifier flip is real. /Users/jamesbrowne/aigame/experiments/rc2_campaign/PREREGISTRATION_DRAFT.md:72-75 reads "in every family with >= 20 valid evaluated genomes, P90 - P10 of T1-PG >= 0.21 ... Fail in any sampled family -> ARCHIVE_KILL". /Users/jamesbrowne/aigame/experiments/rc2_archive/PREREGISTRATION.md:75-78 reads "BAR W passes iff >= 2 sampled families are LIVE". Universal-quantifier kill vs >=2-LIVE pass: confirmed flip.

2. The counterfactual on R2's own recorded data holds exactly — I checked the one escape hatch. The draft raises the family-sampling gate from Phase C's >=15 to >=20 valid genomes, so if R2's threshold family had <20 valid the draft's wording would not fire. It had 23: /Users/jamesbrowne/aigame/experiments/rc2_archive/replicate2x/probe_results.md (Stage 0 table) records threshold n_valid=23, spread 0.0560 below floor ("no"), territory 0.1494 LIVE, connection 0.0739 LIVE, "BAR W: 2 LIVE of 3 sampled -> PASS". Under the draft's quantifier this exact recorded pattern is ARCHIVE_KILL; the recorded verdict was ARCHIVE_GO (RESULTS.md:98-104) — the very run whose B=600 scale the draft adopts as "the validated replicate scale". The counterfactual is structural (grammar applied to the recorded liveness pattern), and it is literally true as claimed.

3. Supporting numbers all recompute. RESULTS.md:103 "threshold DEAD at 0.0560 (was 0.0734 in run 1)" — verified. RESULTS.md:118-121 "threshold's within-family liveness did NOT replicate ... marginal for threshold" — verified. cost_tiering.json:140-143 e1453 T1 per-stream +0.0833 / -0.3333, swing = 0.4166 ≈ 0.4167 — verified, and it is the largest cross-stream swing of all seven anchors (next largest 0.125). T1 threshold anchors S2 +0.0417 (line 64) and e1453 -0.125 (line 139) — verified; their span (0.167) is itself below the draft's own 0.21 BAR W-PG floor, i.e. the signal's two known threshold anchors do not separate by the bar's floor. Additionally /Users/jamesbrowne/aigame/experiments/rc2_planning_gap/ANCHOR_CALIBRATION.md:30-37 documents the mechanism: the UCT rollout model is "wrong enough that more search amplifies its error" on threshold (e1453) boards — PG's instrument is explicitly documented as misspecified there.

4. The draft does NOT handle the concern elsewhere — it makes it worse. The "DRAFT marks for the panel" list flags the 0.21 floor and the >=20 gate for attack but never flags the quantifier change. Worse, the draft's own attributability premise (§1: "One variable changes from ARCHIVE_GO") and the design spec's recorded owner decision #4 (/Users/jamesbrowne/aigame/docs/superpowers/specs/2026-06-12-rc2-campaign-design.md: "Phase C archive machinery verbatim ... no other machinery change") are contradicted by an unflagged second change to the decision grammar. The all-families wording does appear in the spec's bars section (lines 64-65), but it is not one of the four recorded owner decisions, so the finding is not overriding a deliberate recorded owner choice.

5. The proposed fix breaks nothing recorded. Restoring the >=2-LIVE quantifier reproduces R2's recorded PASS; the dead-family slate-exclusion and the re-derive-with-false-kill-rate option are offered as alternatives with "pin which at lock", which is the correct prereg posture.

Severity: blocker stands. ARCHIVE_KILL is a terminal whole-campaign token ("redesign precedes any campaign"; §9 "no archive re-registration"), and the drafted grammar demonstrably converts the lineage's own validated-GO data pattern into that kill, triggered by the family with documented non-replicating liveness, the weakest PG anchors, and the largest measured noise — before any search runs. A prereg about to lock with a verified spurious-kill failure mode in its binding grammar is precisely a blocker. The only soft spot is the rhetorical "modal outcome" claim, which cannot be verified (no fresh-genome per-family PG distributions exist yet) — but it is directionally supported and the finding's materiality does not depend on it.

</details>

<details><summary>Refuter 2 (failed to refute)</summary>

FINDING SURVIVES (not refuted). I attempted refutation along five axes; all failed.

1. Evidence checks out line-for-line. PREREGISTRATION_DRAFT.md:72-75 reads "in every family with >= 20 valid evaluated genomes, P90 - P10 of T1-PG >= 0.21 ... Fail in any sampled family -> ARCHIVE_KILL" — the quantifier flip is exactly as claimed. Phase C's validated grammar at rc2_archive/PREREGISTRATION.md:77-78 is "BAR W passes iff >= 2 sampled families are LIVE / BAR W fail -> ARCHIVE_KILL". RESULTS.md:103 confirms R2 ARCHIVE_GO with "2 of 3 sampled LIVE ... threshold DEAD at 0.0560 (was 0.0734 in run 1)"; RESULTS.md:118-119 confirms "threshold's within-family liveness did NOT replicate". cost_tiering.json:140-143 confirms e1453 T1 per-stream +0.0833 / -0.3333 (swing 0.4166), and T1 threshold anchors S2 +0.0417, e1453 -0.1250 — all exactly as cited.

2. The counterfactual is fully grounded, not hypothetical. I attempted the refutation "threshold might not reach the draft's stricter >= 20-genome qualification gate, making the kill path moot" — it fails: replicate2x/probe_results.md:20 records threshold n_valid = 23 >= 20 with spread 0.0560 < floor. So even under the draft's own family-qualification constant, the recorded R2 data qualifies threshold and fails it; only the quantifier differs, and it alone flips a validated ARCHIVE_GO into ARCHIVE_KILL.

3. The strongest refutation angle — signal mismatch (draft bar is T1-PG/0.21, Phase C data is obs_drama/0.064, so the draft's bar cannot literally be 'applied to' Phase C data) — does not defeat the finding. The reviewer's construction isolates the quantifier as the only delta at the recorded run's own floor/signal, which is the legitimate way to test a grammar change. And under the draft's actual signal the risk is worse, not better: the two threshold PG anchors span only 0.167 (+0.0417 to -0.125), below the 0.21 floor, and threshold's e1453 has the largest cross-stream swing of all 7 anchors (0.4166 vs next-largest 0.125; I recomputed all swings). PG's discriminating anchors (d4015 +0.417, S3 +0.458, S4 -0.417) live in connection/territory, exactly as claimed.

4. The draft does not handle the concern elsewhere. The DRAFT marks invite attack on the 0.21 floor and the >= 20-genome threshold but never flag the quantifier flip; the design spec (docs/superpowers/specs/2026-06-12-rc2-campaign-design.md:64-65) contains the same "all sampled families" wording with no recorded rationale for departing from the validated >= 2-LIVE grammar and no registered campaign-level false-kill derivation (N-of-N compounds per-family false-DEAD rates; 3-sigma was calibrated for a per-family LIVE test feeding a >= 2-of-N rule). Aggravating: the draft itself scopes REACH-v3 threshold-only — it already treats threshold as the pathological family — yet hands that family a whole-campaign veto, and §9 house grammar makes ARCHIVE_KILL non-re-registrable, on what memory records as the sole registered remaining track.

5. The proposed fix breaks nothing recorded. Restoring ">= 2 LIVE" is the Phase C validated grammar verbatim; "dead families excluded from slate-eligible cells" merely constrains §7 slate composition (the d4015 anchor and S3 carry-in are connection-family, unaffected; BAR H is family-agnostic); the re-derivation alternative is standard.

Severity: blocker is correct for a prereg lock review. One overstatement noted — "spurious ARCHIVE_KILL the modal outcome" is not computable from recorded data (no fresh-genome within-family PG spread distribution exists); the recorded base rate is threshold failing its floor in 1 of 2 drama runs, with PG anchor evidence pointing worse. But the finding's force is independent of the exact probability: a non-re-registrable whole-campaign kill, fireable by the documented-marginal noisiest family before any search value is assessed, contradicting the lineage grammar the draft claims to inherit, is a must-fix-before-lock defect. This overstatement is a wording nit, not grounds to downgrade.

</details>

### C4. [BLOCKER] BAR W-PG floor 0.21 is statistically vacuous: the underlying σ≈0.07 exists nowhere on disk, noise-only spread straddles/exceeds the floor, and raw-vs-floored input is unpinned

**Section:** §6 BAR W-PG / §3 noise claim

**Claim:** [3 lenses converged: methodology + degeneracy + repofacts] Floor 0.21 = 3 × claimed σ(T1,n=24)≈0.07, but no σ value is recorded in cost_tiering.json or any planning-gap file — 0.07 is the optimistic mean-|d| estimator reconstructed post hoc. Pooling the T1 per-stream pairs properly (Σd²/(2k) over 7 paired streams) gives σ≈0.087 (full-conv halves ≈0.075; binomial bound 0.102; excluding the e1453 outlier 0.039 — noise is strongly genome/family-dependent). A zero-signal family's P90−P10 is ≈2.56σ ≈ 0.19–0.26: Monte-Carlo at N=20 gives P(noise-only spread ≥ 0.21) = 7% / 39% / 70% at σ = 0.07 / 0.087 / 0.102 — the bar is a coin flip in both directions, and if true σ ≥ 0.082 every family passes LIVE on pure noise so ARCHIVE_KILL is unreachable (kill switch vacuous). The draft also never says whether BAR W uses raw or floored T1-PG: raw lets a zero-depth family pass on exactly the variance the project's own registered rule calls meaningless ('ordering AMONG negative-PG games measures rollout-model misalignment, not relative depth') — the S4/S5 confound pair alone spans 0.458, more than 2× the floor — while floored values collapse spread in negative-heavy families (spurious kill). The same unrecorded σ feeds CAL-I's σ_diff and the BAR H-PG σ≈0.032 note.

**Evidence:** PREREGISTRATION_DRAFT.md:40-41 ('σ ≈ 0.07 at n=24 (cost_tiering per-stream)'), :72-75, :133-134; grep across experiments/rc2_planning_gap/* returns no quantitative σ; cost_tiering.json per_stream blocks (lines 50-52, 65-67, 80-82, 95-97, 110-112, 125-127, 140-143; e1453 +0.0833/−0.3333) → pooled σ24≈0.087; T1 S4 −0.4167 / S5 +0.0417 (raw spread 0.458); ANCHOR_CALIBRATION.md:44-46 (negative-region rule); review recomputation: E[P90−P10 | noise, N=20] = 0.162/0.200/0.236, P(≥0.21) = 0.07/0.387/0.70.

**Proposed fix:** Measure σ(T1,n=24) directly before lock (bootstrap the stored per-game cells per roster game, or one fresh-stream T1 repeat on the seven; ~cheap), record it in a citable file naming the Σd² estimator and the e1453 heteroskedasticity caveat. Pin BAR W to FLOORED T1-PG with rationale stated (in a dead family P10 floors at 0 so the bar demands P90 ≥ floor — true informative-region spread; the noise-null family then shows ≈0.09 and fails cleanly). Set the LIVE floor from a registered Monte-Carlo noise null at the family's actual N (95th percentile of noise-only P90−P10 ≈ 0.28 at N=20, σ=0.087). Re-derive the dependent CAL-I 0.30 and BAR H-PG floor commentary from the recorded σ (full-conv σ for BAR H).

<details><summary>Refuter 1 (failed to refute)</summary>

Finding survives at blocker severity after independent verification of every evidence item. (1) Repofacts confirmed: PREREGISTRATION_DRAFT.md:40 claims "σ ≈ 0.07 at n=24 (cost_tiering per-stream)" but grep across experiments/rc2_planning_gap/ finds no quantitative σ in any file; cost_tiering.json stores only raw per-stream PG pairs at the cited lines (e1453 +0.0833/−0.3333 verified). (2) Recomputation confirmed: proper pooled estimator over the 7 T1 pairs gives σ24=0.0866≈0.087; excluding e1453 gives 0.039; binomial bound 0.102; full-conv halves 0.078 (~reviewer's 0.075); the mean-|d| estimator reproduces 0.0709 exactly, confirming 0.07 is an unrecorded optimistic post-hoc reconstruction, with strong genome-dependent heteroskedasticity (e1453 pair implies σ≈0.21). (3) Monte Carlo independently reproduced: E[P90−P10|noise,N=20]=0.162/0.201/0.236 and P(≥0.21)=0.072/0.397/0.704 at σ=0.07/0.087/0.102 (discrete-lattice simulation slightly worse, 0.476 at σ≈0.087). The statistical core error is real: noise-only spread is already 2.56σ, so the "3σ" floor sits ~0.4σ above the noise expectation — a coin flip bound to ARCHIVE_KILL. (4) Raw-vs-floored is material: §6 never pins the bar input; under raw, the S4/S5 pair alone spans 0.4584 (verified) — passing the validity bar on spread ANCHOR_CALIBRATION.md:43-45 registers as meaningless rollout-misalignment ordering; under floored, the same pair fails. (5) Dependencies confirmed: CAL-I 0.30 = 3×(0.07√2) and BAR H's 0.032 = 0.07/√10×√2 = 0.0313 both reconstruct exactly from the same phantom σ (BAR H also misapplies T1 n=24 noise to full-conv n=48 re-evals). (6) The DRAFT-marks section invites attack on the constant but contains no σ derivation, noise null, or input pin — not prior handling. (7) The proposed fix breaks nothing recorded: ADOPT_T1 rests on A1/A2/Spearman not σ; CAL-I's observed 0.834 clears a re-derived ~0.37 floor; W/D/L cells on disk support the bootstrap; the fix's noise-null floor ≈0.27-0.28 verified. Minor overstatements only ("every family passes if σ≥0.082" is too strong near the boundary — P(pass)≈0.28 at N=20, though ≈0.98 at σ=0.102/N=100; ANCHOR_CALIBRATION cite off by one line; "unpinned" arguably "implicitly raw per §3") — none change the verdict or severity, since a binding kill-switch constant derived from a nonexistent number, vacuous in both directions at plausible σ, in a prereg about to lock, must be fixed pre-lock.

</details>

<details><summary>Refuter 2 (failed to refute)</summary>

NOT REFUTED. Every load-bearing piece of evidence verifies on disk and every statistic reproduces independently. (1) PREREGISTRATION_DRAFT.md:40 claims "σ ≈ 0.07 at n=24 (cost_tiering per-stream)" and :72-74 sets BAR W-PG floor 0.21 = 3×0.07 with ARCHIVE_KILL; yet grep across experiments/rc2_planning_gap/ finds NO quantitative σ anywhere, and the design authority doc (docs/superpowers/specs/2026-06-12-rc2-campaign-design.md:57) calls σ≈0.07 "measured" with no measurement artifact existing — which strengthens, not weakens, the finding. (2) Recomputation from cost_tiering.json T1 per-stream pairs: pooled Σd²/(2k) → σ24=0.0866 (claim 0.087 ✓); excl. e1453 → 0.0390 ✓; binomial bound 0.1021 ✓; the mean-|d| estimator yields exactly 0.0709, confirming 0.07 is only reachable via the optimistic estimator. The BAR H "σ≈0.032" note reproduces only by misapplying σ=0.07 to n=48 full-conv (0.0313), confirming the unrecorded σ propagates into BAR H. (3) Independent Monte Carlo at N=20: E[P90−P10|noise]=0.162/0.201/0.237 and P(≥0.21)=0.071/0.387/0.711 at σ=0.07/0.087/0.102 — matching the reviewer to three decimals; the floor is a near-coin-flip under the on-disk-data σ. (4) Raw-vs-floored is genuinely unpinned: BAR H-PG explicitly says "floored" while BAR W-PG says bare "T1-PG", and ANCHOR_CALIBRATION.md:43-45 contains the negative-region rule verbatim; S4 (−0.4167) and S5 (+0.0417) are both family "territory", so the same-family raw spread 0.458 (>2× floor) is driven by exactly the variance the registered rule deems meaningless, while floored the pair spans 0.042 and fails — both readings pathological. (5) The draft does NOT handle this elsewhere: the DRAFT-marks paragraph invites attack on the 0.21 constant (soliciting this finding, not answering it) and never flags the raw/floored ambiguity or the σ provenance. (6) The proposed fix breaks nothing recorded: BAR W has never executed, CAL-I survives re-derivation (0.834 observed vs corrected ~0.37 bar), and pre-lock σ measurement matches the project's own calibrate-before-spend norm. One sub-clause is overstated — "σ ≥ 0.082 → every family passes / kill switch vacuous" is wrong (my large-N MC gives P(noise passes)=0.40–0.71 at σ=0.082–0.087, N=100–150, and the reviewer's own 39% figure contradicts "every"), and the full-conv-halves figure is 0.0786 not 0.075 — but the accurate core framing ("coin flip in both directions") independently vacates the bar's evidential value. Severity stands at blocker: a binding ARCHIVE_KILL bar with a 39–70% per-family false-pass rate under plausible σ, resting on an unrecorded optimistic estimate the design doc falsely labels "measured", with an ambiguous input variable whose two readings fail in opposite directions, must be fixed before the preregistration locks.

</details>

### C5. [BLOCKER] Stage 0 is unregistered/silently dropped: BAR W's population is undefined (with a vacuous-pass path), quotas contradict the ≥20-valid bar, and M-arm bootstrap is unspecified

**Section:** §2 arms / §6 BAR W-PG

**Claim:** [3 lenses converged: methodology + degeneracy + repofacts (blocker)] The draft names a Stage-0 seed base but registers no Stage-0 protocol: no stop rule, sample size, per-family quotas, attempt/re-draw caps, no arm initialization, and no statement of WHEN BAR W is evaluated. Phase C 'verbatim' stops Stage 0 at ≥15 valid/family (total ≥120, caps 160 evaluated / 2000 attempts) and initializes BOTH arms with all Stage-0 valid genomes — vs the draft's ≥20-valid-per-family bar: whether any family reaches 20 is luck of the draw (run 1 got 99 valid across 3 families), the qualifying set can be empty → BAR W quantifies over nothing → trivially passes → the slate trigger's W leg is satisfied with no validity check performed; or an analyst chooses the population at verdict time. If BAR W is instead computed over campaign output, M-arm selection pressure manufactures within-family spread (selection widens the very percentile gap the bar tests), so a signal with zero fresh-genome validity passes. 'Sampled' in '< 2 families sampled → PROBE_INCOMPLETE' is undefined, and at t=0 'uniform-random filled cell → mutate that cell's elite' is undefined over an empty archive — the arm-fairness property Phase C's reviewer verified depended on identical init. Phase C avoided BAR-W/BAR-H verdict collisions structurally by deciding BAR W at Stage-0 close, which the draft never states.

**Evidence:** PREREGISTRATION_DRAFT.md:17-31 (no Stage 0; :27 is the only mention), :72-75, :97-98; rc2_archive/PREREGISTRATION.md:67-84 (stop rule 15/120/160/2000; :33-35 re-draw cap 50/step; :81-84 'Both arms initialize their archive with ALL Stage-0 VALID genomes'; BAR W computed on Stage-0 valid genomes); rc2_archive/RESULTS.md:52-54 (Stage-0 attrition 265→99 valid across 3 families), :79-81 (reviewer-verified arm fairness depends on identical init).

**Proposed fix:** Reinstate a registered Stage 0: fresh CAL-stream-disjoint sample with quotas raised to match the bar (stop when every sampleable family has ≥20 valid AND total valid ≥150; caps 240 evaluated / 3000 attempts; elimination expected unsampleable, stated), carry over Phase C's re-draw and attempt caps. Both archives initialize from the same Stage-0 valid set. Pin BAR W-PG's population to Stage-0 valid genomes' floored T1-PG only; define qualifying family = ≥20 valid at Stage-0 close; BAR W evaluable iff ≥2 families qualify, else PROBE_INCOMPLETE; families below 20 marked UNSAMPLED and reported (Phase C precedent). BAR W is decided at Stage-0 close so ARCHIVE_KILL preempts the arms and BAR H (explicit precedence).

<details><summary>Refuter 1 (failed to refute)</summary>

NOT REFUTED — the finding survives at blocker severity. Every load-bearing evidence citation verified on disk. (1) Stage 0 is genuinely unregistered: grep confirms the only Stage-0 mention in /Users/jamesbrowne/aigame/experiments/rc2_campaign/PREREGISTRATION_DRAFT.md is the seed base at line 27-28; no stop rule, quotas, caps, arm initialization, or BAR-W timing anywhere (incl. §10). The draft's import pattern is explicitly selective ("Phase C verbatim" for configs, "inherited from Phase C" for eval-count matching, "Phase C checkpoints ×2"), so Stage 0 cannot be claimed as silently inherited — and even full inheritance via the design spec's owner-decision-4 "machinery verbatim" leaves an unresolved contradiction: Phase C's stop rule targets ≥15 valid/family (rc2_archive/PREREGISTRATION.md:69-71) while the draft's BAR W-PG quantifies over families with ≥20 valid (draft:72-75). (2) The contradiction is empirically live, not hypothetical: R2's threshold family landed at exactly 23 valid (replicate2x/probe_results.md) — within sampling noise of the 20 cutoff — and was the family that went DEAD; under the draft's stricter "fail in ANY sampled family → ARCHIVE_KILL" grammar (vs Phase C's "≥2 LIVE passes"), whether the whole campaign is killed could hinge on an unregistered ±4-genome draw accident. (3) "Sampled" is used (draft:74, :98) but never defined (Phase C defined UNSAMPLED = <15 valid); BAR-W timing/precedence vs BAR H is unstated, whereas Phase C resolved it structurally ("Stage 1 does not run", PREREGISTRATION.md:78). (4) M-arm bootstrap is unspecified: "uniform-random filled cell → mutate elite" is undefined over an empty archive at t=0, and the reviewer-verified arm-fairness property explicitly rested on identical Stage-0 init (RESULTS.md:79-83, PREREGISTRATION.md:83). (5) The selection-pressure argument against computing BAR W on campaign output is methodologically sound: Phase C registered BAR W as a fresh-generator-output question; M-arm output measures the search's own movement, not the signal's discriminative validity. One overreach noted, not verdict-changing: the "vacuous pass" branch is partially blocked — under the reading sampled=≥20-valid, the draft's own "<2 families sampled → PROBE_INCOMPLETE" (draft:97-98) closes it, and empirically the qualifying set was never near empty (per-family valid 27/34/38 run 1; 26/48/23 R2) — but the finding frames this as a disjunction whose other branch (undefined term = analyst degrees of freedom at verdict time) stands on its own and is independently disqualifying for a preregistration. The proposed fix breaks nothing recorded: caps 240/3000 are consistent with observed ~62% Stage-0 validity; elimination-expected-unsampleable matches the recorded Phase C finding; BAR-W-at-Stage-0-close matches Phase C structure; flooring only shrinks P90−P10 (conservative) and aligns the bar with the optimized quality. Bonus corroboration: the fix's "fresh CAL-stream-disjoint" requirement would also correct a defect the finding didn't claim — the draft's "fresh, never used by any recorded probe" streams 17M/34M/51M are exactly R2's recorded streams (rc2_archive/PREREGISTRATION_R2.md:9). Severity is correctly blocker: a binding bar whose failure issues ARCHIVE_KILL has an undefined population, the M arm cannot start as written, and the document is about to lock and spend B=1200 evals plus 3 blind tmux teams.

</details>

<details><summary>Refuter 2 (failed to refute)</summary>

Finding SURVIVES at blocker severity. Every evidence citation verified against the files. (1) Stage 0 is genuinely unregistered: grep confirms /Users/jamesbrowne/aigame/experiments/rc2_campaign/PREREGISTRATION_DRAFT.md:27 is the ONLY Stage-0 mention (a seed base) — no stop rule, quotas, caps, arm initialization, or BAR W timing; the design-authority spec (docs/superpowers/specs/2026-06-12-rc2-campaign-design.md) also contains zero Stage-0 content, so nothing handles it elsewhere. (2) The quota contradiction is real and material: Phase C (rc2_archive/PREREGISTRATION.md:69-73) stops at >=15 valid/family with caps 160 evaluated/2000 attempts, vs the draft's >=20-valid BAR W threshold; both RECORDED runs stopped at the 160-cap, not the quota (run 1: 99 valid; R2: 97 valid), and R2's threshold family hit only 23 valid — within sampling noise of 20, with the draft further removing ~30% of generator space (simultaneous-move exclusion). The vacuous-pass path is reachable: BAR W quantifies over 'every family with >=20 valid'; if 2+ families are 'sampled' (term undefined in the draft; Phase C's inherited definition is the 15-valid floor) but none reaches 20, BAR W is vacuously true, the PROBE_INCOMPLETE guard at :97-98 does not fire, and the slate trigger's W leg passes with no validity check. The conditional branch (BAR W over campaign output -> selection-widened spread) is also methodologically correct since M-arm insertion requires strict improvement on floored PG. (3) M-arm bootstrap is unspecified: 'uniform-random filled cell' is undefined over an empty archive; Phase C's explicit both-arms-init-from-Stage-0-valid clause (rc2_archive/PREREGISTRATION.md:83-84) is omitted, and RESULTS.md:79-81 confirms the reviewer-verified arm-fairness property depended on identical init, exactly as cited. (4) Severity is correct by the project's own rules: draft S10 requires a pure decide_verdict with bars transcribed verbatim and locked pre-data — an undefined BAR W population makes that unimplementable without unregistered analyst choices, and one plausible resolution silently disables ARCHIVE_KILL; the draft's own panel marks flag the >=20 threshold for attack. (5) The proposed fix breaks nothing recorded: scaled quotas/caps match recorded attrition (~60% valid yield), 'elimination unsampleable' matches 0-valid in both recorded runs, Stage-0-close BAR W decision matches Phase C precedent, identical init restores verified fairness. One minor quibble: the fix pins BAR W to FLOORED T1-PG, which compresses the negative tail (P10 -> 0) unlike Phase C's raw-spread bar — defensible under the registered informative-region rule but the panel should state it explicitly. Corroborating side-defect found while verifying (outside this finding's scope): the draft's 'fresh, never used by any recorded probe' seed bases 17M/34M/51M/68M are exactly R2's recorded streams (rc2_archive/PREREGISTRATION_R2.md:9), further evidence the draft's Stage-0 line was not reconciled against Phase C records.

</details>

### C6. [BLOCKER] S-GO-2 (+0.5 top-2 vs bottom-2 by full PG) fails its own validation data, is range-restricted by slate construction, and is undefined on ties — GO is unreachable in exactly the success regime

**Section:** §6 SLATE bars S-GO-2 / §7 slate composition

**Claim:** [3 lenses converged: methodology (blocker) + groundtruth (blocker) + repofacts] Three compounding failures. (a) Backtested on the blind-seven — the exact dataset that validated PG — the draft's statistic returns +0.015, not +0.5: top-2-by-full-PG {S3 0.479→blind 3.1, d4015 0.438→3.83} pool to 3.465 vs bottom-2 {S4 −0.323→3.0, e1453 −0.229→3.9} at 3.450; a margin 33× larger than what the validated signal produced over the FULL 0.80-wide PG range. (b) The slate is the top-5 M-elites BY full-conv PG — five games pushed toward the same PG maximum near the +0.5 ceiling (d4015 0.438 and S3 0.479 already sit there); the record already treats a 0.041 gap as 'within joint noise (n=48 each)', n=48 PG is quantized to multiples of 1/96 with no registered tiebreak (the top-2/bottom-2 split is literally undefined on exact ties and noise-ordered otherwise), and the informative-region slope ≈ 2 agent-points/PG-unit means +0.5 requires a ≥0.25 true PG gap that a successful PG-maximizing archive specifically eliminates among its own top-5. (c) GO = S-GO-1 ∧ S-GO-2, so the better the search works the more certainly S-GO-2 fails: the campaign's modal outcome is a pre-baked NO-GO that says nothing about PG. This also inverts graft 10's intent ('within-R21 binary separation bars — R8/A1 above plateau pod'), which separates groups KNOWN to differ, not five maximized siblings.

**Evidence:** PREREGISTRATION_DRAFT.md:88-90, :101-107; BLIND_SEVEN_VALIDATION.md:5-13 table, :17 (validation passed at Spearman +0.286, not a 0.5-margin), :32-34 ('S3 (+0.479) edges d4015 (+0.438). Within joint noise (n=48 each)'); recomputation: top2 3.465 − bot2 3.450 = +0.015; slope (3.83−3.07)/(0.438−0.052) ≈ 1.97 pts/PG-unit; docs/pivot_menu_synthesis_2026-06-10.md graft 10; run21/SUMMARY.md inter-team SD 0.27-0.36 → 6-verdict pooled-mean noise ~0.17.

**Proposed fix:** Give the separation test real contrast and pin its estimator: (1) recompose as top-3 M-elites by full-conv PG + 2 registered low-PG contrast elites (guard-passing, lowest tertile of floored full-conv PG in the M archive); S-GO-2 = pooled mean(top-3 pool, 9 verdicts) − pooled mean(contrast-2 pool, 6 verdicts) ≥ a margin set at ~2σ of the pooled-mean difference (+0.3–0.4 honest at team-verdict SD 0.38, not +0.5) — the faithful graft-10 top-vs-bottom binary separation, with S-GO-1 retaining 3 shots at 4.10. (2) Register the tiebreak (descending full-conv PG, then lexicographic canonical_hash) and the estimator (pooled over verdicts, not mean-of-game-means). (3) Register a minimum-contrast precondition: if full-PG(top pool min) − full-PG(bottom pool max) < 3×σ(n=48) ≈ 0.15, declare SEPARATION_UNDERDETERMINED (reported; slate verdict rests on S-GO-1 + d4015 validity) rather than silent NO-GO. Alternatively: require the #1-by-PG M-elite to land in the agents' top-2 of the slate.

<details><summary>Refuter 1 (failed to refute)</summary>

NOT REFUTED. I attempted to break each prong and could not; every load-bearing number and quote checks out on disk, the draft does not handle the concern elsewhere, and the proposed fix does not contradict any recorded owner decision.

EVIDENCE VERIFICATION (all confirmed):
(a) Backtest arithmetic: /Users/jamesbrowne/aigame/experiments/rc2_planning_gap/BLIND_SEVEN_VALIDATION.md lines 7-13 give exactly the claimed (PG, blind) pairs. Top-2 by full PG = S3 (+0.479, 3.1) and d4015 (+0.438, 3.83) → pooled 3.465; bottom-2 = S4 (−0.323, 3.0) and e1453 (−0.229, 3.9) → 3.450; diff = +0.015 (recomputed). PG range 0.802; 0.5/0.015 ≈ 33×. The validation PASS was Spearman +0.286 vs a bar of merely >0 (line 17) — nowhere near supporting a +0.5 pooled-mean margin. The "within joint noise (n=48 each)" quote for the 0.041 S3-d4015 gap is verbatim at lines 32-34.
(b) Range restriction + ties: PREREGISTRATION_DRAFT.md §7 (lines 101-107) confirms the slate is the top-5 M-elites BY full-conv PG; §3 confirms full-conv PG is n=48 with draws=0.5, so it is quantized to multiples of 1/96 ≈ 0.0104 (recomputed); no tiebreak is registered anywhere in the draft (the only collision rule, "next-best cell substitutes," is for cell collisions, not PG ties). Slope (3.83−3.07)/(0.438−0.052) = 1.97 recomputed; even at the most favorable concordant-pair slope (~2.6, S2-d4015) the +0.5 bar needs a ≥0.19 true PG gap among top-5 elites pushed toward a hard +0.5 ceiling where references already sit at 0.438/0.479 — structurally implausible exactly when the search succeeds. run21/SUMMARY.md SD column is 0.27-0.36 as claimed; SE of a 6-verdict pooled-mean difference at SD 0.31 = 0.31·√(2/6) ≈ 0.179 ≈ the claimed ~0.17.
(c) Conjunction: §6 lines 91-93 confirm GO requires BOTH bars, either fail → NO-GO with the semantics "PG returns to analysis with which/why" — i.e., a structurally-forced S-GO-2 failure would be recorded as evidence against PG. Graft 10 (docs/pivot_menu_synthesis_2026-06-10.md lines 42, 100) is verbatim "within-R21 binary separation bars (R8/A1 above plateau pod; no cross-campaign Spearman)" — separation of groups known to differ, which the draft's five-maximized-siblings instantiation indeed inverts.

REFUTATION ATTEMPTS THAT FAILED:
1. "The backtest is out-of-population (a real slate excludes e1453-type low-PG games; excluding e1453 the same statistic gives +1.08)." True, but this escape concedes prong (b): if the bottom-2 are also high-PG, the expected separation is slope × (compressed gap) ≈ ≤0.2-0.3, still under +0.5 even noise-free. The two prongs close each other's exits: wide-range slate → inversion risk (the +0.015 regime); compressed slate → no contrast. Either way the +0.5 margin is unsupported by any data on record.
2. "Cross-cell selection gives spread (per-cell maxima differ)." Top-5-by-PG across many occupied cells are upper order statistics that compress toward the global max in the success regime; the claim is precisely scoped to that regime ("unreachable in exactly the success regime"), and a wide-spread top-5 is the search-mediocre regime, not the one GO is supposed to certify.
3. "The draft already handles it." No: the DRAFT-marks section (line 137) lists "S-GO-2 margin +0.5" among constants to attack — an acknowledgment of uncertainty, not a handling — and it flags only the margin constant, not the range-restriction, tie/estimator-undefinedness, or conjunction-structure defects, which are the finding's real content.
4. "The fix breaks a recorded decision." Checked docs/superpowers/specs/2026-06-12-rc2-campaign-design.md: the four recorded owner decisions are the 4.10 threshold, B=600, one slate/3 teams/~7 games, and the drop-in maximand swap. The fix preserves all four (7-game slate, 3 teams, 4.10, conjunction with a separation bar in the graft-10 spirit). "Top-5 M-elites" and the +0.5 margin live in spec/draft detail, explicitly open to panel grafts pre-lock ("a pinned margin" in the spec, pinned only in the draft). The only real tradeoff is S-GO-1 dropping from 5 to ~3 realistic shots at 4.10, which the fix itself discloses; that is an owner tradeoff to flag, not a breakage.

SEVERITY: blocker is correct for a prereg-lock review. The binding GO rule cannot deliver its advertised inference: the better the PG-maximizing search works, the more certainly S-GO-2 fails for reasons orthogonal to PG validity, and the registered verdict semantics would then mislabel search success as signal failure ("PG returns to analysis"). A decision rule whose modal output misattributes its cause must be fixed before lock. Minor caveats (the "+0.015 fails its own validation data" framing is rhetorically strong given the out-of-population application, and "modal outcome" is a well-supported inference rather than a measured fact) do not reduce materiality.

</details>

<details><summary>Refuter 2 (failed to refute)</summary>

NOT REFUTED. I attempted to break the finding on every axis and it survives; severity "blocker" is appropriate for a prereg about to lock.

EVIDENCE VERIFIED (all recomputed from the cited files):
(a) Backtest: /Users/jamesbrowne/aigame/experiments/rc2_planning_gap/BLIND_SEVEN_VALIDATION.md lines 5-13 give exactly the claimed values. Top-2 by full PG = S3 (+0.479 → blind 3.1) and d4015 (+0.438 → 3.83), pooled 3.465; bottom-2 = S4 (−0.323 → 3.0) and e1453 (−0.229 → 3.9), pooled 3.450; difference = +0.015. The validation verdict (line 17) passed at Spearman +0.286 > 0 — a sign test, never a +0.5-margin demonstration. Line 32-34 confirms "S3 (+0.479) edges d4015 (+0.438). Within joint noise (n=48 each)" — the record itself treats a 0.041 PG gap as noise.
(b) Range restriction: PREREGISTRATION_DRAFT.md §7 (lines 101-107) composes the slate as "Top-5 M-arm elites by full-convention PG" — five games selected on the same maximized statistic, with PG hard-bounded at +0.5 and known games already at 0.438/0.479. Slope recomputed: (3.83−3.07)/(0.438−0.052) = 1.97 agent-pts/PG-unit, so +0.5 needs a ≥0.254 true PG gap between top-2 and bottom-2 of the top-5 — a gap a successful PG-maximizing archive specifically eliminates. n=48 PG is quantized to 1/96 ≈ 0.0104 (verified arithmetic) and per-game σ(n=48) ≈ 0.05, so within a compressed top-5 the top-2/bottom-2 ordering is noise-ordered and exact ties at the rank-2/3 or 3/4 boundary are live. I re-read the full draft: NO tiebreak is registered anywhere (§7's collision rule is cell-based, not PG-tie-based), and the estimator (pooled vs mean-of-means; whether d4015/S3 count as "slate elites") is genuinely ambiguous.
(c) GO conjunction: §6 says GO requires S-GO-1 AND S-GO-2, either fail → NO-GO with "PG returns to analysis" — so in the success regime (compressed top-5 PG), S-GO-2 passes only on noise (SE of the 6v-vs-6v pooled difference ≈ 0.22 at verdict SD 0.38 → +0.5 is a 2.3σ event, ~1%). The modal outcome is a structurally pre-baked NO-GO carrying a misleading verdict about PG. Graft 10 verified verbatim in docs/pivot_menu_synthesis_2026-06-10.md (line 42 and line 100): "within-R21 binary separation bars (R8/A1 above plateau pod)" — separation of groups KNOWN to differ; S-GO-2 inverts this onto five maximized siblings. run21/SUMMARY.md inter-team SDs 0.27/0.36 confirmed.

REFUTATION ATTEMPTS THAT FAILED:
(1) "Backtest is not apples-to-apples" (blind-seven contains negative-PG, guard-failing games that could never be slate elites): true, but it cuts the wrong way — restricting the backtest to guard-passing nonneg-PG games (drop RUSH-flagged S1) gives top-2 {S3,d4015}=3.465 vs bottom-2 {S5,S2}=3.135 → +0.33, still failing +0.5; the only slice clearing +0.5 (+1.08) requires the guard-vetoed S1 in the bottom pool. The +0.015 headline is the most adversarial slice, but the conclusion is robust across all plausible slices.
(2) "Draft already handles it": the DRAFT-marks section flags only the +0.5 CONSTANT for panel attack; the structural failures (slate-construction range restriction, undefined ties, unpinned estimator, success-regime unreachability of the GO conjunct) are flagged nowhere in the draft or in docs/superpowers/specs/2026-06-12-rc2-campaign-design.md (which records the same top-2/bottom-2 structure with the margin merely "pinned" later).
(3) "Fix breaks something recorded": the fix's contrast-elite recomposition does deviate from the spec's recorded owner slate decision (top-5 M-elites; top-2/bottom-2 form) and so needs owner re-decision, and its 3σ(n=48)≈0.15 precondition is loose (3×σ_diff for two n=48 measurements is ≈0.21; 0.15 is the single-measurement figure). These are fix-quality caveats for the graft discussion, not refutations of the finding.

MINOR CAVEATS (do not change verdict): "GO unreachable" is ~99%-unreachable, not literally impossible (noise can clear 2.3σ); and the graft-10-inversion point partially indicts the design spec's own owner decision, not only the draft. Neither softens blocker severity: locking the prereg as drafted bakes a structurally unsatisfiable GO conjunct into a campaign whose stated question is whether PG-driven search works, guaranteeing an uninformative NO-GO precisely when it does.

</details>

### C7. [BLOCKER] BAR H-PG floor 0.05 collides with PG's hard +0.5 ceiling — conditionally unreachable in exactly the regime PG-maximization produces

**Section:** §6 BAR H-PG

**Claim:** PG is capped at +0.5 by construction (mean score − 0.5). The generator space at this exact budget has ALREADY produced a 0.479 full-conv genome without even selecting on PG: S3 is an R2 archive elite (drama-selected, B=600, same generator) and d4015 sits at 0.438. Headroom above S3 (0.021) is less than half the 0.05 floor. Both arms select directly on PG with 600 evaluations each; R's top-10 are the 10 best of 600 PG-screened draws, so the expected operating point crowds the cap, and max achievable top10(M)−top10(R) = 0.5 − R_top10_mean. If R's top-10 mean reaches 0.45, BAR H-PG is arithmetically unreachable and the campaign lands SEARCH_NEUTRAL regardless of real search value. Phase C documented precisely this compression with drama ('top-10 mean compresses exactly where the two arms compete hardest') — and drama had no hard cap; PG does. The draft's own panel note worries the 1.5σ floor is too LOW; the ceiling makes it simultaneously too HIGH.

**Evidence:** PREREGISTRATION_DRAFT.md:76-79, :135-136; cost_tiering.json full_reference: S3 +0.479, d4015 +0.4375 (S3 = Phase D 'S' slate game = R2 arm-M elite, rc2_descriptor_v2/PREREGISTRATION.md:36-38); rc2_archive/RESULTS.md:32-36 (run-1 saturation mechanism); PG max = +0.5 by definition (anchor_calibration.py:15-17).

**Proposed fix:** Register a saturation contingency pre-data: report R_top10 mean alongside BAR H-PG; if R_top10 ≥ a pinned saturation constant (e.g., 0.40), the binding search-value metric switches to a registered ceiling-robust alternative — paired per-cell wins on jointly filled cells (already computed, currently 'reported not binding') or logit-transformed deep-win share — with its own pre-derived floor. Do not let SEARCH_NEUTRAL be reachable by ceiling arithmetic alone.

<details><summary>Refuter 1 (failed to refute)</summary>

FINDING SURVIVES at blocker severity. I attempted refutation on every axis and failed on all of them.

EVIDENCE VERIFICATION (all checks pass):
1. PG hard cap +0.5: confirmed at /Users/jamesbrowne/aigame/experiments/rc2_planning_gap/anchor_calibration.py lines 14-17 — PG = mean score − 0.5 with score in {0, 0.5, 1}, so PG ∈ [−0.5, +0.5] by construction. The campaign's full-conv re-eval (draft §3) uses the same convention.
2. Reference values: /Users/jamesbrowne/aigame/experiments/rc2_planning_gap/cost_tiering.json full_reference gives S3 = 0.47917 (47/48 deep wins — one game from the cap) and d4015a646ae3 = 0.4375. Headroom above S3 = 0.0208 < 0.025 (half the 0.05 floor), exactly as claimed.
3. S3 provenance: confirmed via /Users/jamesbrowne/aigame/evaluations/rc2_phase_d/PREREGISTRATION.md lines 10-20 — S3 = 0165399e5aef, taken from R2 arm M's final archive (drama-selected, B=600/arm, GameGeneratorV2 default space — the identical generator and budget the campaign registers in §2). So the search-reachable space at this exact budget demonstrably contains a 0.479-PG genome found WITHOUT selecting on PG.
4. Draft §6 (PREREGISTRATION_DRAFT.md:76-79): BAR H-PG = top10(M) − top10(R) ≥ 0.05 on floored full-conv PG, the SOLE binding search-value bar; slate gated on it; fail → SEARCH_NEUTRAL. Lines 133-135 confirm the panel note worries only the too-LOW direction ("is 1.5σ enough?").
5. Phase C precedent: /Users/jamesbrowne/aigame/experiments/rc2_archive/RESULTS.md:32-36 contains the quoted compression mechanism verbatim ("top-10 mean compresses exactly where the two arms compete hardest").
6. Arithmetic: max achievable gap = 0.5 − R_top10_mean. At R_top10 = 0.45 the bar is reachable only in the degenerate case of all 10 M-elites at exactly +0.5 (perfect 48/48 each); above 0.45 it is strictly unreachable. The claim's "arithmetically unreachable at 0.45" is a measure-zero overstatement, not a refutation.

REFUTATION ATTEMPTS THAT FAILED:
- "Draft handles it elsewhere": No. I grepped the entire rc2_campaign dir and the design-authority doc (docs/superpowers/specs/2026-06-12-rc2-campaign-design.md) for saturation/ceiling/R_top10 handling — nothing. §8 reports raw PG distributions and per-cell paired wins non-bindingly, but the binding token on compression is still SEARCH_NEUTRAL.
- "R2 passing BAR H at B=600 shows compression self-resolves": this cuts the OTHER way. R2 passed (+0.0574) because arm M broke out of the territory-family soft ceiling into family headroom (RESULTS.md R2 section: top-10 went 5 territory / 4 connection / 1 threshold). PG's cap is family-universal and mathematical — there is no breakout dimension above +0.5. Drama's ceiling was escapable; PG's is not.
- "Condition is a tail event, so blocker is over-severitized": not defensible pre-data. PG is a win-share metric that saturates once depth dominates; both tactically-live reference games (S3 0.479, d4015 0.4375) sit within 0.07 of the cap, and the campaign selects the 10 best of 600 PG-screened draws per arm. Worse, the registered SEARCH_NEUTRAL escalation (§6: heritability r ≥ 0.3 → 2×-budget replicate) INHERITS and AGGRAVATES the failure — more budget pushes both arms closer to the hard cap, unlike drama's R2 where 2× budget opened family headroom. The failure mode is reachable, campaign-invalidating (false "search adds no value" verdict + 8h burn), unrecoverable post-lock, and cheap to guard against — textbook prereg blocker.
- "Fix breaks something recorded": No. The document is explicitly DRAFT/pre-lock; §9's "no archive re-registration" house grammar governs post-run verdicts, not pre-data design. Per-cell paired wins are already computed by the Phase C machinery (R2 jointly-filled record 20W/4T/6L), and R2's own "registered next" flagged "whether top-K is the wrong binding metric for archive value" as an open design input — the fix aligns with the recorded lineage. Two minor caveats for the fix author, neither overturning the finding: (a) logit-transformed deep-win share is infinite at 48/48 and needs a registered smoothing constant; (b) the saturation constant must itself be pinned pre-data with a derivation, or it becomes a new attackable free parameter.

</details>

<details><summary>Refuter 2 (failed to refute)</summary>

REFUTATION FAILED — the finding is real, the evidence checks out line-by-line, and blocker severity is justified for a pre-lock review.

Evidence verification (all confirmed on disk):
1. PG hard cap +0.5 by construction: /Users/jamesbrowne/aigame/experiments/rc2_planning_gap/anchor_calibration.py:14-17 — PG = mean score − 0.5 with score ∈ {0, 0.5, 1}, so PG ∈ [−0.5, +0.5]. Flooring at 0 (draft §3) only raises negatives; the +0.5 ceiling is inescapable. Measured pinning at the exact cap occurs in practice: d4015 scored exactly 0.5 at T3 (cost_tiering.json, 12/12 wins).
2. Reference values: /Users/jamesbrowne/aigame/experiments/rc2_planning_gap/cost_tiering.json full_reference — S3 = 0.4792, d4015 = 0.4375. Headroom above S3 = 0.0208 < 0.025 (half the 0.05 floor). Arithmetic of conditional unreachability is correct: top10(M) ≤ 0.5 always, so max diff = 0.5 − R_top10; at R_top10 = 0.45 the max diff is exactly 0.0499... < 0.05.
3. S3 provenance: /Users/jamesbrowne/aigame/evaluations/rc2_phase_d/.blind_mapping.json — S3 = archive elite, cell [connection, 4, 2], from R2 arm-M (Phase D elite source = replicate2x/probe_results.csv per rc2_archive/RESULTS.md). Drama-selected, B=600, GameGeneratorV2/MutationOperatorV2 DEFAULT — the campaign's exact space. Crucially, S3 survives the campaign's one new exclusion (simultaneous-move quick-reject): S3 was PG-evaluated by the UCT instrument, which by the draft's own rationale cannot evaluate simultaneous-move genomes, so S3 is in-scope.
4. Phase C precedent: rc2_archive/RESULTS.md:32-36 says verbatim "top-10 mean compresses exactly where the two arms compete hardest" — and the token bound anyway ("Declared as a metric limitation, not re-litigated: the token is NEUTRAL"). House grammar honors bound tokens even when post-hoc analysis shows metric saturation, so "raw PG distributions reported not binding" (§8) is observability, not mitigation.
5. Draft does NOT handle it: grep across PREREGISTRATION_DRAFT.md and docs/superpowers/specs/2026-06-12-rc2-campaign-design.md finds no saturation contingency; per-cell paired wins are explicitly non-binding (§8); the DRAFT-marks panel note (lines 133-135) attacks the floor only from below ("is 1.5σ enough?").

Why blocker (not major) stands — the structural point my refutation could not break: drama's Phase-C compression was against a SOFT family ceiling, and the registered remedy (2×-budget replicate) worked because more budget pushed arm M past it (R2: gap reopened to 0.0574, family-diverse top-10). PG's ceiling is a hard constructive cap, and the draft's registered SEARCH_NEUTRAL remedy is the SAME 2×-budget replicate (§6 / spec lines 66-70) — which under cap saturation is arithmetically counterproductive: best-10-of-1200 random draws ≥ best-10-of-600 in expectation, so R_top10 rises and max achievable diff shrinks further. The sole binding search-value bar can land SEARCH_NEUTRAL by ceiling arithmetic alone, on the program's sole registered track, with a registered escape path that provably cannot recover. Additionally, the win-rate metric loses all resolution in [0.45, 0.5] (quantized at 1/96 ≈ 0.0104 at n=48) — exactly the band PG-maximization drives arm M into.

Honest weaknesses found (do not overturn): (a) the saturation condition R_top10 ≥ 0.45 is undemonstrated — the closest analogy (R2 drama: R top-10 0.2331 ≈ 76% of known-best 0.306) extrapolates to R_top10 ≈ 0.36 and ~0.13 headroom, i.e., the bar may well be reachable; (b) cross-cell max-1-per-cell dilutes R's raw order statistics; (c) the claim's "and d4015 sits at 0.438" slightly overreaches — d4015 is the R8 anchor from genesis_v2_run8.db, NOT a product of this generator space (only S3 is). But the finding is framed conditionally ("conditionally unreachable"), the load-bearing example (S3) is exactly as claimed, and prereg review exists precisely to close foreseeable verdict-grammar holes pre-lock — by this project's own recorded standards (Phase C token discipline; descriptor-v2 kill; the draft's invitation to attack BAR H-PG hardest).

Proposed fix checked against the record — it breaks nothing: per-cell paired wins are already computed and listed reported-not-binding (§8), and Phase C's run-1 honest synthesis itself flagged "top-K-on-drama is the wrong binding metric for archive value" as a design input, so a registered ceiling-robust conditional alternative aligns with lineage. It does not violate owner decision 4 (drop-in maximand — the fix touches the verdict bar, which the DRAFT-marks explicitly open to panel attack) nor "no archive re-registration" (this is pre-lock drafting, not post-data re-registration). One implementation nit: logit-transformed deep-win share is infinite at win-share 1.0 and needs a continuity correction; the primary alternative (paired per-cell wins) has no such issue.

Verdict: finding survives at blocker severity. Recommend grafting the saturation contingency (report R_top10 alongside BAR H-PG; pinned saturation constant ~0.40 triggering a pre-derived ceiling-robust binding alternative) before lock.

</details>

### C8. [MAJOR] REACH-v3 ported from the full convention to T1 without revalidation — its only validated positive (S2) sits exactly on the 0.25 firing boundary at the operating tier

**Section:** §4 REACH-v3 (operating convention)

**Claim:** [3 lenses converged: methodology + degeneracy + repofacts] REACH-v3 was validated under the FULL PG convention — play_cell defaults UCT@256 vs UCT@16, n=48, fresh streams 44/45 — where S2's draw share is 0.417 and e1453 is silent at 0.000. The draft applies the same ≥0.25 threshold to 'the genome's own T1 games' (128v16, n=24), a different play convention where S2's only on-disk measurement is exactly 6/24 = 0.25: the guard's single validated positive sits ON the firing boundary (binomial σ ≈ 0.088 at n=24; one fewer draw → 0.208 → silent), so at T1 it fires on S2 at roughly coin-flip rates. Draw propensity is convention-dependent (deeper search resolves differently — COST_TIERING reading 2), so 'validated on fresh streams 44/45; zero extra cost' overstates what was validated: the convention changed along with the cost claim.

**Evidence:** reach_v3.py:13-16, :59-64 (STREAMS 44/45, DRAW_SHARE 0.25, n=48) calling anchor_calibration.play_cell at defaults DEEP_SIMS=256/SHALLOW_SIMS=16 (anchor_calibration.py:93-94, 142-144); REACH_V3.md:7 (S2 0.417 FIRES, e1453 0.000); cost_tiering.json T1 S2 wins/draws/losses = 10/6/8 → 0.250 exactly at threshold; PREREGISTRATION_DRAFT.md:55-57; COST_TIERING.md:34-39.

**Proposed fix:** Re-validate at the campaign instrument pre-lock: one cheap reach_v3 rerun at deep_sims=128 (or play S2 + e1453 at T1 on streams 46/47, ~7 CPU-min) requiring fires-on-S2 / silent-on-e1453 at the registered T1 threshold (e1453 shows 0 draws at T1, so the negative margin is huge). Re-price the constant for n=24 so the validated positive sits ≥1σ inside the firing region (e.g., fire on draws ≥ 5/24 ≈ 0.21) with the binomial false-fire rate stated — or compute REACH from a dedicated n=48 / full-conv re-eval record for insertion candidates only, where the validation applies as-is.

<details><summary>Refuter 1 (failed to refute)</summary>

NOT REFUTED — finding verified end-to-end and survives at major severity. (1) Convention mismatch is real: reach_v3.py (lines 60-64) validated the guard at the FULL convention — play_cell defaults DEEP_SIMS=256/SHALLOW_SIMS=16 (anchor_calibration.py:93-94, 142-144), n=48, fresh streams 44/45 — where S2 = 0.417 FIRES and e1453 = 0.000 (REACH_V3.md:7-8). PREREGISTRATION_DRAFT.md §4 (lines 55-56) applies the same ≥0.25 threshold to "the genome's own T1 games" (UCT@128 vs 16, n=24 per §3) while citing the 44/45 validation as warrant; the design spec repeats the conflation; no T1 re-validation of REACH exists anywhere (CAL-I covers only PG separation). (2) Boundary arithmetic confirmed: cost_tiering.json T1 S2 = 10W/6D/8L → 6/24 = 0.250 exactly at the inclusive boundary; recomputed σ = 0.0884 at n=24, one fewer draw → 0.208 → silent; P(fire | true p=0.25, n=24) = 0.578 — coin-flip claim accurate. (3) Convention-dependence confirmed in the harmful direction: S2 draw share 0.354 (256v16, streams 42/43, blind_seven_validation.json 17/48) and 0.417 (256v16, 44/45) vs 0.250 (128v16) — the operating tier pushes the only validated positive onto the boundary (0.90 fire rate if full-conv propensity transferred; on-disk T1 datum says it doesn't). (4) Material at major: COST_TIERING.md reading 3 makes REACH-v3 the explicit load-bearing mitigation for T1's weakest property ("S2 gets flagged on draw share regardless of where its PG lands") — that mitigation is the thing shown to be coin-flip; the guard is a binding insertion veto defending against the campaign's exemplar threshold-family Goodhart pathology, and the registered text's validation claim is inaccurate for the operating instrument. (5) The proposed fix breaks nothing recorded: the draft is explicitly unlocked pending panel grafts; at T1 only S2 has ≥5 draws among the seven anchors (S1=3, all others 0), so re-pricing to ≥5/24 stays consistent with recorded data, and e1453's T1 negative margin (0 draws) is huge as claimed. Only trivial nits: e1453's row is REACH_V3.md line 8 not 7, and the "0.25 is a priori" defense in reach_v3.py does not rescue the draft's wording, which leans on the streams-44/45 validation as warrant.

</details>

<details><summary>Refuter 2 (failed to refute)</summary>

REFUTATION FAILED — the finding is real, the evidence chain verifies end-to-end, and major severity is warranted.

Evidence verification (all recomputed from disk):

1. Validation convention confirmed: /Users/jamesbrowne/aigame/experiments/reach_endcause/reach_v3.py defines STREAMS=(44,45), DRAW_SHARE=0.25, N_BINDING=24/stream (n=48) at lines 60-63, and submits play_cell WITHOUT overriding sims (lines 95-97), so it inherits DEEP_SIMS=256/SHALLOW_SIMS=16 from /Users/jamesbrowne/aigame/experiments/rc2_planning_gap/anchor_calibration.py:93-94 (play_cell defaults, lines 142-144). REACH_V3.md line 7-8: S2 draw share 0.417 (n=48) FIRES; e1453 0.000. So the guard was validated only at the FULL convention.

2. Operating convention confirmed: PREREGISTRATION_DRAFT.md §3 sets screening at T1 = UCT@128 vs UCT@16, n=24; §4 lines 55-56 applies the same ≥0.25 threshold to "the genome's own T1 games" and asserts "(validated on fresh streams 44/45; zero extra cost)". That validation never ran at T1 — the parenthetical overstates what was validated.

3. Boundary fact confirmed: cost_tiering.json tier T1, game S2 = wins 10 / draws 6 / losses 8, n=24 → 6/24 = 0.250, exactly on the firing boundary; one fewer draw (5/24 = 0.208) is silent. A repo-wide grep shows the cost_tiering files are the ONLY records at the 128-sim tier, so this is S2's only on-disk T1 measurement. Recomputed binomial: σ = sqrt(.25·.75/24) = 0.0884 (claim said ≈0.088, correct); P(fire | p=0.25, n=24) = 0.578 — "roughly coin-flip" is accurate. Convention-dependence is also supported by the data, not just COST_TIERING reading 2: P(X≤6 | n=24, p=0.417) ≈ 0.07, i.e., the T1 draw share is marginally inconsistent with the full-conv value, and S2's PG itself moves +0.198 → +0.042 → −0.125 across tiers (COST_TIERING.md lines 34-39, verified).

4. Not handled elsewhere: the draft's panel-marks list flags a DIFFERENT REACH concern (threshold-only scoping); CAL-I (§5, streams 46/47) revalidates the PG instrument, not the guard; the design-authority spec (docs/superpowers/specs/2026-06-12-rc2-campaign-design.md, ~line 50) repeats the identical overstated wording, so the defect originates upstream and is unaddressed everywhere.

5. Materiality supports MAJOR: the guard is a binding insertion veto, and COST_TIERING.md reading 3 explicitly uses REACH-v3 as the backstop justifying ADOPT_T1 despite T1's thin S2 PG margin ("S2 gets flagged on draw share regardless of where its PG lands") — at T1 that flagging is only ~58% reliable on the sole validated positive, so the T1 adoption rationale itself partially rests on an undemonstrated property. Given this campaign family's documented failure mode (Goodharted degenerate elites: drama first-turn-win, S4/S5 parity races), an under-firing draw guard at the operating convention reopens the exact S2-style pathology channel the guard exists to close, pre-lock, with a factually overstated validation sentence in registered text. That is squarely "major, fix before lock."

6. Fix sanity-check: the proposed fixes are cheap and sound; e1453's negative margin at T1 is enormous (0 draws in 24 at T1 per cost_tiering.json), so a T1 revalidation is nearly free. One footnote: the fix variant "play S2+e1453 at T1 on streams 46/47" would consume stream numbers the draft reserves as fresh for CAL-I (§5) — different genomes so no game reuse (and play_cell seeds derive from stream/idx only, not game key, so identical rng streams would be shared), but streams 48/49 would honor the freshness grammar more cleanly. This wrinkle affects one fix variant only and does not rescue the finding.

</details>

### C9. [MAJOR] T1 screening and full-conv re-evals pooled into one ledger is an incoherent estimator: instrument offsets are 2–3× the claimed noise and every bar's input is ambiguous

**Section:** §3 quality signal / elite re-evals / §6 BAR H-PG

**Claim:** [3 lenses converged: methodology + degeneracy + repofacts] Screening inserts on T1 (128v16, n=24) while re-evals are full-convention (256v16, n=48), and the draft inherits Phase C's single-instrument 'stored value = pooled mean' plus challenger eval-count matching verbatim without defining how cross-instrument values pool or compare. The two instruments measure systematically different quantities per game/family — S2: +0.042 (T1) vs +0.198 (full); S4: −0.417 vs −0.323; e1453: −0.125 vs −0.229 — offsets of 0.09–0.16, i.e., 2–3× the claimed σ. If the pools mix: stored elite values become history-dependent (shift at every checkpoint, threshold incumbents re-priced UP), T1 challengers fight mixed-convention incumbents whose displacement difficulty is a function of how many re-evals they happened to receive rather than quality, and BAR H's 'mean floored full-conv PG … post-final-re-eval pooled' is not actually full-conv. If they don't mix: 'pooled' and 'eval-count matching inherited from Phase C' are undefined across the two pools. Either reading leaves the central quality bookkeeping unspecified at lock and breaks bit-identical reproducibility of the bars. Phase C never faced this because its re-eval batches were the same instrument as screening.

**Evidence:** PREREGISTRATION_DRAFT.md:39-47 ('full-convention PG … with challenger eval-count matching inherited from Phase C'), :76-78 ('full-conv PG … post-final-re-eval pooled'); cost_tiering.json: S2 T1 +0.0417 (line 64) vs full_reference +0.1979 (lines 27-33); e1453 T1 −0.125 (line 139) vs full −0.2292; COST_TIERING.md:13-21 per-game table; rc2_archive/PREREGISTRATION.md:47-55 (single-instrument pooled-mean + matching semantics being inherited).

**Proposed fix:** Two registered ledgers per genome, never mixed: (1) T1 ledger — all screening/top-up batches; used for insertion comparisons, challenger fights, and eval-count matching, within-instrument only; (2) full-conv ledger — written only at checkpoint re-evals; read by top-10 selection, BAR H-PG, and slate selection. Pin which ledger is the stored cell value, the rule for elites lacking a full-conv batch at a checkpoint (e.g., receive one before bar computation), and transcribe which ledger every bar reads at which time.

<details><summary>Refuter 1 (failed to refute)</summary>

VERDICT: NOT REFUTED. The core finding is real, the evidence checks out line-for-line, and it is material at major severity — though two quantitative framings in the claim are inflated and should be corrected in the graft.

EVIDENCE VERIFICATION (all exact):
- /Users/jamesbrowne/aigame/experiments/rc2_planning_gap/cost_tiering.json: S2 T1 +0.0417 (line 64) vs full_reference +0.1979 (line 32); S4 −0.4167 (line 94) vs −0.3229 (line 27); e1453 −0.125 (line 139) vs −0.2292 (line 30). Offsets −0.156, −0.094, +0.104 as claimed. COST_TIERING.md:13-21 table matches.
- PREREGISTRATION_DRAFT.md §3 (lines 37-47) and §6 BAR H-PG (lines 76-78) read as quoted.
- rc2_archive/PREREGISTRATION.md:46-55 confirms the inherited Phase C semantics: "Quality = ... pooled mean over all batches received", "stored value = new pooled mean" after re-evals, "ALL bar computations use post-final-re-eval pooled means", and the eval-count-matching rule — all single-instrument in Phase C (every batch n=50, same protocol). The claim "Phase C never faced this" is correct.

THE AMBIGUITY IS REAL AND UNRESOLVED ELSEWHERE: I checked the design-authority spec (docs/superpowers/specs/2026-06-12-rc2-campaign-design.md), which the draft defers to for owner decisions — it does NOT resolve the ledger question; it makes it worse. Owner decision 4 ("Phase C archive machinery verbatim ... no other machinery change") plus §3's full-conv re-evals forces the mixed-pool reading (T1 n=24 batches pooled with full-conv n=48 batches into one stored mean), while BAR H-PG's wording "mean floored full-conv PG ... post-final-re-eval pooled" contradicts mixing (a mixed pool is not full-conv PG). The reviewer's dichotomy is exact: mix → stored values are history-dependent instrument blends (an early elite ends at 86% full-conv weight, n=24+3×48; a late or challenge-topped-up one materially less) and BAR H is mislabeled; don't mix → "pooled" and "eval-count matching inherited from Phase C" (which compares challenger first-batch vs incumbent POOLED mean and matches pooled n) are undefined across pools. Either way the runner cannot be built bit-identically from the locked text without an unregistered choice. Notably, COST_TIERING.md's own validated protocol says "Full convention stays reserved for survivors" — i.e., the validated design envisioned separated instruments, supporting that the Phase-C "pooled" transplant is an unintended verbatim carry-over.

MATERIALITY AT MAJOR: BAR H-PG is one of two binding bars (gates the slate vs SEARCH_NEUTRAL). After the draft's own flooring rule (quality = max(PG,0)), the only offsets that survive are in the positive region — and the largest one (S2, threshold family, floored 0.042 vs 0.198 = 0.156) sits exactly there. 0.156 is 3.1× the BAR H floor (0.05) and 5× the draft's own stated top-10-mean σ (0.032). Instrument-composition differences across a handful of top-10 elites can therefore swing BAR H by more than its floor. This is the campaign's central quality bookkeeping in a document whose stated purpose is pre-data bit-identical decision rules; major is the right severity.

OVERSTATEMENTS THE GRAFT SHOULD CORRECT (insufficient to refute):
1. "Offsets are 2–3× the claimed σ" is wrong arithmetic: 0.094/0.07=1.34×, 0.104/0.07=1.49×, 0.156/0.07=2.23× — i.e., 1.3–2.2× σ(T1), and only 1.1–1.8× the correct comparison noise σ_diff≈0.086 (sqrt(0.07²+0.049²)); no offset reaches 3σ and individually none is statistically decisive. The "systematic offset" framing nevertheless stands because the repo's own registered reading (COST_TIERING.md "search depth IS the signal"; S2's monotone collapse +0.198→+0.042→−0.125→−0.208 across four tiers) treats it as a real depth effect, so "it's just noise" is not an available refutation.
2. "Every bar's input is ambiguous" is overbroad: CAL-I and BAR W-PG are T1-only and unaffected; the ambiguity hits stored cell values, challenger displacement, BAR H-PG, and §7 slate-composition ranking.
3. "Threshold incumbents re-priced UP" holds only for positive-PG threshold games (S2-like); e1453 re-prices DOWN (immaterial post-flooring, 0 either way).
4. The fix's clause for "elites lacking a full-conv batch" is largely moot: Phase C verbatim re-evaluates EVERY elite at the final checkpoint (rc2_archive/PREREGISTRATION.md:53-54), so all top-10/slate candidates have ≥1 full-conv batch at bar time.

FIX CHECK: the two-ledger proposal breaks nothing recorded — it aligns with cost_tiering's validated "full convention reserved for survivors" separation, keeps BAR H's floor conservative (full-conv n=48 noise is smaller than the T1 noise the 0.05 floor was sized against), and makes eval-count matching well-defined within-instrument. One trade-off worth noting in the graft: a strict two-ledger design removes Phase C's re-eval de-noising of stored T1 values (lucky n=24 insertions persist unless challenged), so the graft should state that re-pricing of the stored cell value, if any, happens within the T1 ledger only or is explicitly forgone.

</details>

<details><summary>Refuter 2 (failed to refute)</summary>

NOT REFUTED — finding is real, material, and correctly severitized as major; only its quantitative headline is overstated.

Evidence verified against the repo: (1) /Users/jamesbrowne/aigame/experiments/rc2_campaign/PREREGISTRATION_DRAFT.md lines 37-47 confirm screening = T1-PG (128v16, n=24, σ≈0.07) while elite re-evals = full-convention PG (256v16, n=48) "with challenger eval-count matching inherited from Phase C"; lines 76-78 confirm BAR H reads "mean floored full-conv PG ... post-final-re-eval pooled". (2) /Users/jamesbrowne/aigame/experiments/rc2_planning_gap/cost_tiering.json confirms every cited number: S2 T1 +0.0417 (line 64) vs full +0.1979 (line 32), S4 −0.4167 vs −0.3229, e1453 −0.125 (line 139) vs −0.2292 (line 30); recomputed offsets 0.156/0.094/0.104. (3) /Users/jamesbrowne/aigame/experiments/rc2_archive/PREREGISTRATION.md lines 46-55 confirm the inherited Phase C semantics are single-instrument: quality = pooled mean over ALL batches, challenger top-ups until pooled n ≥ incumbent pooled n, re-eval "stored value = new pooled mean", "ALL bar computations use post-final-re-eval pooled means" — and Phase C's screening/top-up/re-eval batches were all the same obs_drama instrument (n=50/100), so the cross-instrument pooling question genuinely never arose there.

The draft does NOT handle the concern elsewhere. The design-authority spec (docs/superpowers/specs/2026-06-12-rc2-campaign-design.md) makes the incoherent reading the default: owner decision #4 is "drop-in swap. Phase C archive machinery verbatim ... no other machinery change", and its architecture section re-lists "re-eval noise pricing, challenger eval-count matching" as verbatim inheritances while separately introducing the different-instrument re-eval. Under verbatim inheritance, full-conv n=48 batches pool into stored T1 values (mixed-instrument, history-dependent stored values; BAR H's "full-conv" label false); under the no-mix reading, "pooled" and "eval-count matching" are undefined across two pools and the stored-value update at re-eval is unspecified (§8's "re-eval re-pricing magnitudes" shows stored values are expected to change). No runner code exists in experiments/rc2_campaign/ (directory contains only the draft) and grep finds no ledger definition anywhere. The draft's own panel-attack list (lines 131-141) omits this issue. Corroborating the confusion: the draft prices BAR H noise as "σ ≈ 0.032 under measured T1 noise" (= 0.07·√2/√10) for a bar nominally computed from n=48 full-conv values — the draft itself mixes the instruments in its noise pricing.

Materiality supports MAJOR: BAR H is a binding gate (slate vs SEARCH_NEUTRAL) with floor 0.05, smaller than the measured S2 instrument offset (0.156), so the ambiguity is decision-relevant; insertion/eval-count-matching semantics govern the entire archive; and §10's pure-decide_verdict + bit-reproducibility obligation cannot be satisfied with an ambiguously defined estimator. The proposed two-ledger fix deviates from owner decision #4, but the spec's process note explicitly anticipates panel grafts before lock, so the fix breaks nothing recorded (its contingency for elites lacking a full-conv batch is largely moot if Phase C's every-elite-per-checkpoint re-eval is inherited, but pinning that rule is exactly the kind of transcription the fix asks for).

Two imprecisions, neither severity-reducing: (1) the headline "offsets are 2–3× the claimed σ" overstates — ratios to σ=0.07 are 2.2× (S2), 1.5× (e1453), 1.3× (S4), none reaching 3×, and against the proper σ_diff ≈ 0.086 for a T1-vs-full comparison they are only 1.1–1.8σ, individually compatible with noise; the systematicity claim instead rests (validly) on the monotone tier trend (S2: +0.198→+0.042→−0.125→−0.208; S4 and e1453 similar) and COST_TIERING.md's own mechanism reading that "search depth IS the signal". (2) "Threshold incumbents re-priced UP" is direction-mixed (S2-like up, e1453-like down). The core defect — the central quality estimator, challenger matching rule, and binding-bar inputs undefined across two instruments at lock — stands even at zero offset, so the finding survives at major severity. A correct restatement of the headline would be "offsets up to ~2.2× the claimed σ (1.8× σ_diff), with a monotone tier trend indicating systematic instrument dependence".

</details>

### C10. [MAJOR] Slate constraint 'cross-cell, max 1 per cell' is dead text in a MAP-Elites archive — one family of near-identical genomes can fill all 5 slate seats

**Section:** §7 slate composition

**Claim:** [2 lenses converged: groundtruth + repofacts] In the inherited Phase C archive each cell holds exactly one elite (challenger displaces incumbent), so ANY 5 elites are automatically from 5 distinct cells: the clause and its 'next-best cell substitutes on collision' rule constrain nothing (collisions cannot occur) while supplying false diversity comfort. Adjacent cells of the same family (neighboring interaction/length bins) can hold near-duplicate genomes — the exact failure run21 documented (G2 'nominal PASS / functional FAIL': 4 menger games 'near-identical', differing 'only by komi and an inert max_turns cap', i.e., distinct canonical hashes, one kernel). Phase C's own record shows family monoculture at the top (run 1: both arms' top-10 100% territory; R2 M top-10 still 5/10 territory), and the PG anchors make connection the top-PG family (S3 +0.479, d4015 +0.438) — so the plausible slate is 5 near-duplicate connection elites + d4015 (connection) + S3 (connection): 7/7 one family, 21 verdicts spent on ~1–2 kernels, and S-GO-2's contrast destroyed.

**Evidence:** PREREGISTRATION_DRAFT.md:101-103; rc2_archive/PREREGISTRATION.md:40-57 (one elite per (family, interaction_bin, length_bin) cell; dedup by canonical_hash only); evaluations/run21/SUMMARY.md:36 (G2 finding, quoted); rc2_archive/RESULTS.md:33 ('both arms' top-10 are 100% territory-family') + R2 section ('5 territory / 4 connection / 1 threshold'); ANCHOR_CALIBRATION.md table (connection holds the top PG values).

**Proposed fix:** Replace the dead clause with two registered constraints applied in PG order with next-best substitution (all substitutions reported): (1) family cap — max 2 of the 5 elites per win-condition family (next-best by full PG from an unrepresented family substitutes on excess); (2) near-duplicate screen — a candidate is skipped if it differs from an already-selected elite only in parameters with no behavioral effect (registered check: identical family + identical board/topology + descriptor_row distance below a pinned floor, or rules-diff limited to komi/max_turns). This directly encodes the run21 G2 lesson into the slate builder, mechanical at verdict time.

<details><summary>Refuter 1 (failed to refute)</summary>

NOT REFUTED — the finding is real, the evidence checks out, and major severity is warranted. (1) Dead clause verified in code: evolution/qd_archive.py stores exactly one Elite per (family, interaction_bin, length_bin) cell and each genome maps to exactly one cell, so the top-5 M-arm elites are automatically from 5 distinct cells; the §7 clause 'max 1 per cell; next-best cell substitutes on collision' (PREREGISTRATION_DRAFT.md:102-103) can never bind — collisions are impossible. The identical dead clause also appears in the design spec (docs/superpowers/specs/2026-06-12-rc2-campaign-design.md:81). (2) The near-duplicate adjacent-cell pathway is verified end-to-end: operators_v2.py:536-541 perturbs max_turns ±10-20% in _mutate_win_condition; qd_archive.py cell_key divides mean game length by max_turns, so an inert cap change leaves play identical while shifting the length bin into an empty adjacent cell (first occupancy needs no improvement test); game_def_v2.py canonical_blob includes win_condition, so the variant has a distinct canonical_hash and the hash-only dedup (rc2_archive/PREREGISTRATION.md:56-57) cannot catch it; an inert variant inherits its parent's PG up to noise and lands beside it in the top-5. This is exactly the run21 G2 failure, quoted accurately from evaluations/run21/SUMMARY.md:36. (3) Family-monoculture precedent verified: rc2_archive/RESULTS.md line 32 (run 1: both top-10s 100% territory) and line 110 (R2 M top-10: 5 territory / 4 connection / 1 threshold); PG anchors verified — S3 +0.479 and d4015 +0.438 are the top two PG values and both connection (S3's value is in BLIND_SEVEN_VALIDATION.md, not ANCHOR_CALIBRATION.md as cited — a minor citation slip; the numbers are real). (4) The draft handles the concern nowhere else: no duplicate/kernel/diversity language anywhere in the draft or spec; §8 only reports top-10 family composition non-bindingly; the panel-attack list omits slate composition. (5) Materiality at major: the slate is the campaign's sole ground-truth stage (21 blind verdicts); a near-duplicate top-5 structurally destroys S-GO-2's top-2-vs-bottom-2 contrast (near-identical games give near-identical team means), forcing a spurious NO-GO that wrongly sends PG — the only registered quality-signal track — back to analysis on a slate-construction artifact. Two caveats that do not change the verdict: (a) the '7/7 one-family slate' is a labeled-plausible projection, not a certainty (Phase C R2's M top-10 was family-diverse under drama), but the near-duplicate mechanism and S-GO-2 confound do not require full monoculture; (b) the proposed family cap (max 2 of 5) can be unsatisfiable when only 2 families hold elites (elimination was 0-valid in both Phase C runs, threshold marginal), so the fix needs a registered exhaustion fallback — a refinement of the fix, not a refutation of the finding.

</details>

<details><summary>Refuter 2 (failed to refute)</summary>

NOT REFUTED. Every load-bearing claim verified against the repo; the finding is real and "major" is the right severity.

1. DEAD TEXT — CONFIRMED IN CODE. /Users/jamesbrowne/aigame/evolution/qd_archive.py:158 declares `self.cells: dict[tuple[str,int,int], Elite]` — exactly one elite per (family, interaction_bin, length_bin) cell; `offer()` displaces the incumbent on win. Therefore any top-5 elites by PG are automatically from 5 distinct cells: the §7 clause "max 1 per cell; next-best cell substitutes on collision" (PREREGISTRATION_DRAFT.md:102-103) can never bind and the substitution rule can never fire. Mathematically vacuous, as claimed.

2. NEAR-DUPLICATE-ACROSS-CELLS MECHANISM — CONFIRMED IN CODE, stronger than the reviewer stated. cell_key (qd_archive.py:124-137) computes length_frac = mean_length / win_condition.max_turns, so an INERT max_turns change (game never reaches the cap) moves a behaviorally identical genome into a different length bin — parent and near-identical child can co-exist as elites in adjacent cells. MutationOperatorV2._mutate_win_condition perturbs max_turns ±10-20% (evolution/operators_v2.py:536-541), so arm M actively generates these. Dedup is canonical_hash only (rc2_archive/PREREGISTRATION.md:56-57). This is precisely the run21 G2 failure: SUMMARY.md:36 verbatim — "nominal PASS / functional FAIL — ... 4 menger games are near-identical (e52e≈bfd1≈1fea differ only by komi and an inert max_turns cap)". One nuance the reviewer missed (doesn't weaken the finding): komi-only variants don't move the descriptors, so they compete for the SAME cell and self-dedup; the live channel is max_turns and other behavior-inert binning-denominator parameters — which the finding explicitly names.

3. MONOCULTURE PRECEDENT — CONFIRMED IN RECORD. rc2_archive/RESULTS.md line 32 (cited as 33; off-by-one, trivial): run 1 "both arms' top-10 are 100% territory-family"; R2 M top-10 = "5 territory / 4 connection / 1 threshold". PG anchors: S3 +0.479 and d4015 +0.438 (both connection) are the top two PG values — S3's value lives in rc2_planning_gap/BLIND_SEVEN_VALIDATION.md:10, not ANCHOR_CALIBRATION.md as cited (minor citation slip; ANCHOR_CALIBRATION.md does hold d4015 +0.438 as table top). Substance checks out.

4. NOT HANDLED ELSEWHERE IN THE DRAFT. §8 "family composition of top-10s" is reported-not-binding; there is no family cap, no behavioral near-dup screen anywhere; the "DRAFT marks for the panel" section does not list slate composition among the items to attack. The d4015 validity band cannot rescue it — an out-of-band result triggers a replicate slate built by the same flawed composition rule.

5. MATERIALITY = MAJOR, upheld. The slate is the campaign's verdict-bearing stage (21 agent-team verdicts, the binding S-GO-1/S-GO-2 bars). S-GO-2 compares top-2-by-PG vs bottom-2-by-PG WITHIN the 5 slate elites; a near-duplicate slate makes that contrast ~0 by construction → mechanical false NO-GO → the registered consequence fires ("PG returns to analysis; periodic-agent-slates-only becomes the registered selection fallback") — a strategic redirection caused by slate construction, not by the hypothesis under test. The project's own record (run21 G2) shows this exact failure consuming a full eval campaign. A registered diversity clause that cannot bind, masking an absent real constraint at the most expensive stage, is exactly what prereg review exists to catch.

6. FIX SAFETY. The draft is explicitly "NOT LOCKED"; the fix contradicts no locked document or recorded result. One edge case worth flagging when grafting: elimination was 0-valid in both Phase C runs, so realistically ≤3 families hold elites — the "max 2 per family" cap is satisfiable for 5 seats with 3 families (2+2+1) but needs a registered fallback if only 2 families hold positive-PG elites (cap would allow only 4 seats). Also, the family cap widens the slate's PG range, which makes S-GO-2 easier to pass and shifts its meaning from within-top discrimination toward cross-family discrimination — the panel should consciously accept or re-derive the +0.5 margin under the new composition. These are fix refinements, not refutations.

Minor errata (do not change the verdict): S3 +0.479 cited to the wrong file in the same directory; RESULTS.md line off by one.

</details>

### C11. [MAJOR] The stage3_ab blinding convention leaves prior verdicts of the slate's own games readable — d4015 ('Connection Go', 4.10/3.83) and S3 (3.10, unblinded identity table) are discoverable by evaluator teams

**Section:** §7 blinding ('Blind-packed per evaluations/stage3_ab convention')

**Claim:** stage3_ab's out-of-bounds list is only .blind_mapping.json, play.py source, experiments/, and git (BRIEFING.md:6-9). It does NOT exclude evaluations/r8_replay/ (d4015's named briefing + 5 verdicts + the 4.10 mean), evaluations/rc2_phase_d/ (the UNBLINDED identity table with d4015 3.83 and S3 3.10, plus full per-game verdicts), evaluations/run21/, or analysis_post_r21.md. The protocol's own Phase 4 ('Novelty Adversary: attempt to reduce the game to a known prior') actively pushes teams toward exactly the comparison-hunting that surfaces these files, and the briefing hands teams the 'R8 4.10' anchor — so a team that recognizes the anchor game can peg it, neutering the validity band (which only measures drift if d4015 is judged fresh) and pre-loading S3's open PG-vs-depth question with its prior 3.10. Phase D itself recognized the stage3_ab list was insufficient and custom-extended it (forbade evaluations/run21/) — yet still left r8_replay readable; the draft regresses to the weaker list by citing stage3_ab verbatim.

**Evidence:** evaluations/stage3_ab/BRIEFING.md:6-9 (out-of-bounds list), :53-56 (Phase 4 instruction), :80 (scoring anchors include 'R8 4.10'); evaluations/r8_replay/SUMMARY.md (game named 'Connection Go', mean 4.10, full rules briefing in-directory); evaluations/rc2_phase_d/RESULTS.md:16-26 (unblinded identity table); evaluations/rc2_phase_d/PREREGISTRATION.md:42-44 (Phase D's extended-but-still-incomplete list).

**Proposed fix:** Register the out-of-bounds list in this prereg rather than by reference: everything under evaluations/ EXCEPT the new pack directory, plus experiments/, docs/, analysis*.md, memory files, and all git/repo metadata. Add a mandatory recognition-disclosure line to the verdict template ('If you believe you can identify this game or recall a prior score for it, say so and continue') so residual contamination is measured, and have the orchestrator grep filed verdicts for identifier strings (d4015, Connection Go, S3, run8) before unblinding.

<details><summary>Refuter 1 (failed to refute)</summary>

UPHELD at major. I re-verified every cited artifact and could not refute the finding on evidence, materiality, or fix-safety.

EVIDENCE — all checks pass:
(1) /Users/jamesbrowne/aigame/evaluations/stage3_ab/BRIEFING.md:6-9 — the out-of-bounds list is exactly as claimed: .blind_mapping.json, play.py source, experiments/, and git/repo metadata. Nothing under evaluations/ is excluded. Line 80 hands evaluators "R8 4.10, R19 4.375 (top 5.0), R20 3.73, R21 3.69" as scoring anchors. The Phase 4 novelty-adversary instruction exists (actual lines 56-58, not 53-56 — a trivial citation offset that doesn't change anything).
(2) /Users/jamesbrowne/aigame/evaluations/r8_replay/SUMMARY.md names d4015a646ae3 "Connection Go" with mean 4.10, and briefing_r8_d4015a646ae3.md in the same readable directory contains the full verbatim rules JSON plus engine scores — enough to match a blind game by mechanics alone.
(3) /Users/jamesbrowne/aigame/evaluations/rc2_phase_d/RESULTS.md:16-26 is the unblinded identity table exactly as claimed: F = d4015 (prior 4.10) → 3.83; E = S3 → 3.10, with genome summaries (S3 = "connection moore-3D CA") that map observed mechanics directly to identity and prior score. rc2_phase_d/PREREGISTRATION.md:42-44 confirms Phase D extended the prohibition list in-prereg (adding evaluations/run21/ and helper source) yet still left r8_replay readable.
(4) The draft (§7) cites "per evaluations/stage3_ab convention" by reference only; the design-authority spec (docs/superpowers/specs/2026-06-12-rc2-campaign-design.md:25,100) does the same. The concern is handled nowhere else — §7's "teams see no provenance, scores, or signal values" describes pack contents, not on-disk readability.

A structural point the reviewer understates: the stage3_ab list could never have excluded rc2_phase_d/ — that directory postdates the convention. Citing the convention by reference inherits a boundary that is stale by construction; every completed campaign adds new readable contamination (including the exact band center 3.83 the draft's §6 validity band [3.48, 4.18] is built on, and S3's full per-game verdicts with discovered tactics). Phase D's in-prereg enumeration was the house precedent; the draft regresses from it.

MATERIALITY at major — upheld. The validity band is binding (CAMPAIGN_UNRESOLVED gate), and a contamination-induced false PASS would launder drifted teams' verdicts into the binding S-GO-1/S-GO-2 decisions. Recognition requires no rule-breaking: nothing in the briefing's letter forbids reading evaluations/r8_replay/ or rc2_phase_d/, and the Phase-4 prior-hunting task makes such a search letter-compliant. The fix is zero-cost pre-lock, which is exactly the prereg panel's standard for must-fix (= major).

Two mitigating caveats the panel should record, neither severity-reducing: (a) empirically, across 25 prior blind verdicts under identical exposure (stage3_ab ×4, phase_d ×21 with d4015 in-slate), grep finds zero contamination — Phase D's anchor scored 3.83 with 4.1/4.0/3.4 spread, clearly not pegged to the readable 4.10 — so realized risk to date is nil; the defect is ex-ante protocol unsoundness, not observed misconduct. (b) The proposed orchestrator grep must carve out "R8"/"run8": compliant verdicts quote the briefing-supplied "R8 4.10" anchor verbatim (e.g., rc2_phase_d/team-3_gameF.md:121), so those strings false-positive; "d4015" and "Connection Go" are clean discriminators. The rest of the proposed fix (in-prereg out-of-bounds enumeration: all of evaluations/ except the new pack, experiments/, docs/, analysis*.md, memory, git; plus a recognition-disclosure line) breaks nothing recorded and matches the Phase D precedent.

</details>

<details><summary>Refuter 2 (failed to refute)</summary>

NOT REFUTED — finding is real, evidence verified, and major severity is correctly calibrated. (1) Evidence checks out on every citation: stage3_ab/BRIEFING.md:6-9 forbids only .blind_mapping.json, play.py source, experiments/, and git — no evaluations/* directory; line 80 hands teams the 'R8 4.10' anchor; Phase 4 (lines 56-58, citation off by 2 lines) instructs teams to hunt for known priors. evaluations/r8_replay/ contains the d4015 briefing with the VERBATIM rule JSON including unique float fingerprints (strength 0.7149717563606287, decay 0.7510267575649336) — matching a blind game's --rules output to this file yields definitive identification, plus the 4.10 mean and the name 'Connection Go'. evaluations/rc2_phase_d/RESULTS.md:16-26 contains the unblinded identity table (d4015 3.83, S3 3.10) exactly as claimed — and this file post-dates Phase D, so RC2's contamination surface is strictly larger than any prior campaign's. Phase D's briefing (lines 9-13) and prereg (42-44) confirm it custom-extended the out-of-bounds list (added run21/ and stage3_ab/) yet still left r8_replay readable; the RC2 draft §7 registers only 'per evaluations/stage3_ab convention' — the weaker reference — and handles the concern nowhere else (the 'teams see no provenance, scores, or signal values' clause describes pack contents, not repo readability; the design spec repeats the same bare reference). (2) Materiality at 'major' holds: the §6 validity band [3.48, 4.18] contains both publicly readable prior values (4.10 and 3.83), so a recognizing team that pegs the anchor passes the band silently, vacating the campaign's only instrument-drift guard; S-GO-1's 4.10 threshold rides the same scale; the protocol has no recognition-disclosure or contamination detection, so the failure is undetectable post-hoc. (3) The strongest counter-evidence — Phase D's actual d4015 scores 4.1/4.0/3.4 (mean 3.83, team-3 well below any peg; no verdict references r8_replay) show contamination did not visibly occur under identical exposure — argues only that exploitation hasn't happened yet, not that the registered protocol guarantees blindness; prereg review evaluates the guarantee, and Phase D's own list-extension shows the project already judged the stage3_ab list insufficient. (4) The proposed fix breaks nothing recorded: scoring anchors are in the briefing itself, teams need only the new pack directory, and the disclosure line + pre-unblind grep are additive measurement. Minor quibbles that do not change the verdict: the line citation :53-56 for Phase 4 is off by ~2 lines; 'regresses to the weaker list' is slightly rhetorical since §7 cites a convention rather than reproducing a list — but the registered reference IS the weaker artifact, and even Phase D's stronger list leaves the r8_replay channel open, so the substance stands either way.

</details>

### C12. [MAJOR] Decision grammar is not exclusive/complete: W∧H both-fail precedence, wall-cap scope vs the slate, and cap-hit salvage are unspecified

**Section:** §6 / §9 decision grammar

**Claim:** (1) If BAR W and BAR H both fail, the draft assigns two verdicts (ARCHIVE_KILL and SEARCH_NEUTRAL) with no precedence — Phase C avoided this structurally by deciding BAR W at Stage-0 close ('Stage 1 does not run'), which the draft never states. (2) The 8h cap's scope is undefined relative to the slate: the slate is owner-token tmux work on a separate timeline; as written, a cap-hit during/after a passed search but before slate completion maps to PROBE_INCOMPLETE, discarding collected verdicts. (3) No salvage rule at cap-hit: with the cost arithmetic showing cap-hit is likely, a run stopped at eval 580/600 yields PROBE_INCOMPLETE with zero bar reads despite passing both registered checkpoints — Phase C's checkpoint machinery exists precisely to allow a registered partial-read. (4) S-GO-1's 'blind team mean' is ambiguous at 1 verdict/team/game and S-GO-2's tie-handling on near-ceiling PG is unspecified (converges with the dedicated S-GO-1/S-GO-2 findings).

**Evidence:** PREREGISTRATION_DRAFT.md:72-79 (independent fail branches, no order), :97-98 (cap → PROBE_INCOMPLETE, scope unstated), :85-87 ('21 verdicts' = 1 per team per game); rc2_archive/PREREGISTRATION.md:78 ('Stage 1 does not run' — the exclusivity mechanism the draft dropped), :36 (checkpoint every 25 evals).

**Proposed fix:** Pin a verdict precedence chain evaluated in order: PROBE_INVALID → PROBE_INCOMPLETE → ARCHIVE_KILL → SEARCH_NEUTRAL → slate verdicts. Define the cap as search-phase wall only (slate excluded; slate completion failure gets its own SLATE_INCOMPLETE handling). Register cap-hit salvage: if both arms passed the 400-eval checkpoint, bars are evaluated at the last mutual checkpoint with B_effective reported, else PROBE_INCOMPLETE. Define S-GO-1 as mean of the 3 team verdicts per game and pin PG tie-breaking (lexicographic on canonical_hash) for S-GO-2 pools.

<details><summary>Refuter 1 (failed to refute)</summary>

NOT REFUTED — the finding survives at major, but two of its four legs fail and the fix needs trimming. VERIFIED CORE (sub-claim 1): PREREGISTRATION_DRAFT.md:74 maps BAR W-PG fail → ARCHIVE_KILL and :81 maps BAR H-PG fail → SEARCH_NEUTRAL with no 'BAR W pass ∧' conjunct; rc2_archive/PREREGISTRATION.md had BOTH exclusivity devices and the draft dropped both (:78 verbatim 'BAR W fail → ARCHIVE_KILL; Stage 1 does not run.'; :109 'BAR W pass ∧ BAR H fail → ARCHIVE_NEUTRAL'). The draft is worse than claimed: it contains no Stage-0 protocol at all (only a reserved seed base, :27), so BAR W's evaluation timing/sample is undefined and W∧H both-fail is genuinely reachable; 'Phase C verbatim' cannot rescue it because the draft restates the grammar with deltas (B 300→600, cap 10h→8h, family threshold 15→20), and §10's pure decide_verdict is unimplementable from a non-exclusive grammar without an unregistered decision. Two verdicts → two different registered next-step paths, so this is lock-blocking = major. SUB-CLAIM 2 (cap scope): real in the draft text but overdrawn — the design authority (docs/superpowers/specs/2026-06-12-rc2-campaign-design.md:100-101) records the 8h cap as a COMPUTE wall ('Estimated compute 1.5–3 hr wall (cap 8 hr, 7 workers); owner tokens: 3 tmux teams'), and CAL-C projects from the per-genome pipeline; the claimed discard-slate-verdicts failure mode contradicts the recorded scoping. One-line graft, minor alone. SUB-CLAIM 3 REFUTED: (a) 'cap-hit is likely' is false — recorded estimate 1.5–3 hr vs 8 hr cap (2.7–5.3x headroom) plus CAL-C pre-launch re-scope gate; (b) 'Phase C checkpoint machinery exists precisely to allow a registered partial-read' is false — rc2_archive:36 says checkpoints are '(resumable)' and Phase C's own grammar (:112-114) maps cap-hit to 'PROBE_INCOMPLETE: partial tables, no verdict token', i.e., Phase C registered NO salvage either, so the draft's no-salvage cap IS the validated house grammar; (c) the draft's 200/400/600 are elite re-eval points, not pass/fail checkpoints — 'passed the 400-eval checkpoint' is a category error; (d) the proposed salvage rule would violate owner decision 4 ('no other machinery change') and the campaign's one-variable-change attributability argument, so that part of the fix breaks something recorded. SUB-CLAIM 4: real but minor and self-admittedly duplicative of dedicated panel findings (21 verdicts = 3 teams × 7 games checks; 'team mean' collides with R21 house usage; full-conv PG granularity 1/96 with +0.5 ceiling makes ties possible). DISPOSITION: severity major stands on sub-claim 1 (+2 as adjunct); strike sub-claim 3 and the salvage portion of the fix (replace with Phase C resume-from-checkpoint language); keep the precedence chain (consistent with Phase C semantics); define the cap as search-compute wall per the design spec; defer S-GO-1/S-GO-2 wording to the dedicated findings.

</details>

<details><summary>Refuter 2 (failed to refute)</summary>

VERDICT: Finding survives at major, carried by sub-claim (1), though sub-claim (3)'s precedent argument is mischaracterized and sub-claim (4) is conceded-duplicative. I attempted refutation on every sub-claim; the core one holds against all angles.

EVIDENCE CHECK (all citations verified on disk):
- /Users/jamesbrowne/aigame/experiments/rc2_campaign/PREREGISTRATION_DRAFT.md:72-79 — BAR W-PG "Fail in any sampled family → ARCHIVE_KILL" and BAR H-PG are independent bullets with no conjunction and no ordering. Line 84 region: "BAR H-PG fail → SEARCH_NEUTRAL" is stated UNCONDITIONALLY. Lines 97-98: PROBE_INCOMPLETE = "wall cap 8 h hit; either archive < 10 elites; < 2 families sampled; anchor games unloadable" — scope unstated. Lines 85-87: "3 independent blind tmux teams, 21 verdicts" with a 7-game slate (top-5 M + d4015 + S3) = exactly 1 verdict/team/game; reviewer arithmetic correct.
- /Users/jamesbrowne/aigame/experiments/rc2_archive/PREREGISTRATION.md:78 — "BAR W fail → ARCHIVE_KILL; Stage 1 does not run." exact quote, exact line. :36 — "Global wall cap 10 h → PROBE_INCOMPLETE. Checkpoint every 25 evals (resumable)." exact.

SUB-CLAIM (1) — SURVIVES, and is independently stronger than stated. grep confirms the draft contains zero occurrences of "Stage 1"/"does not run" and no decision-grammar section at all; Phase C's locked grammar (rc2_archive PREREGISTRATION.md:102-116) used BOTH exclusivity mechanisms — explicit conjunctions ("BAR W pass ∧ BAR H fail → ARCHIVE_NEUTRAL", :109-111) AND temporal staging (:78). The draft dropped both: its SEARCH_NEUTRAL branch lacks the "W pass ∧" guard, and it registers a Stage-0 seed base (line 28) without ever specifying a Stage-0 protocol or when BAR W is decided ("≥ 20 valid evaluated genomes" could read as Stage-0-only or all-campaign-evals). So W∧H both-fail is reachable under a legal reading and maps to two verdicts. This is material at major: §10 mandates a pure decide_verdict "with all branches synthetically tested" — unimplementable without an unregistered precedence choice — and ARCHIVE_KILL vs SEARCH_NEUTRAL drive divergent registered next steps (redesign vs heritability→possible 2×-budget replicate of a within-family-invalid design). A same-pattern gap exists for W-fail ∧ archive<10. Refutation attempts that failed: bullet order is not "applied verbatim" grammar; the parenthetical "redesign precedes any campaign" mirrors Phase C's consequence wording but does not pin evaluation timing; lineage citation of "Phase C machinery" does not import Phase C's staging text.

SUB-CLAIM (2) — real but weaker than presented. CAL-C (lines 65-68) projects "campaign wall" from the per-genome pipeline and re-scopes pre-launch against "the cap", and the other PROBE_INCOMPLETE conditions are all search/CAL-phase conditions — by company, the 8h cap reads as search-phase wall, making the "cap fires mid-slate, discarding verdicts" scenario an implausible reading. Still formally unscoped in a verbatim-grammar document; a one-line scoping fix is warranted. On its own this is minor.

SUB-CLAIM (3) — evidence MISCHARACTERIZED, materiality overstated. Phase C's checkpoints are annotated "(resumable)" — crash-resume machinery, NOT partial-read machinery; Phase C's own grammar makes cap-hit "PROBE_INCOMPLETE: partial tables, no verdict token" (:113-114), i.e. the house precedent is explicitly NO salvage. The claim "Phase C's checkpoint machinery exists precisely to allow a registered partial-read" is false. Also "cap-hit is likely" is not established: CAL-C exists precisely to re-scope B before launch if projection exceeds cap, making a 580/600 cap-hit a projection-error tail event, and the draft itself flags "the 8 h cap vs CAL-C projection" as a panel-attack item (line 137). Absence of salvage is accurately reported but is a defensible house-grammar choice, not an unspecified gap.

SUB-CLAIM (4) — the finding itself concedes it "converges with the dedicated S-GO-1/S-GO-2 findings"; under panel dedup it adds zero marginal weight here. On merits: with 1 verdict/team/game, "blind team mean" most naturally means mean of the 3 team verdicts (matching R21/SIEGE reporting convention); minor clarification nit.

PROPOSED FIX — adopt the precedence chain and cap scoping; CAUTION on the salvage graft: evaluating bars "at the last mutual checkpoint with B_effective" conflicts with the recorded owner decision B=600 ("the validated replicate scale", lines 25-26) and with CAL-C's registered rule that budget changes happen by re-registration BEFORE launch, not silently after; BAR H's σ≈0.032 calibration and top-10 composition also shift with B. Salvage should be an explicit owner decision at lock, not a default graft. The S-GO-2 lexicographic-hash tie-break is harmless.

NET: real, evidence checks out (with one false precedent characterization inside sub-claim 3 that does not touch the core), and material at major via sub-claim (1) — the draft dropped Phase C's entire locked decision-grammar section and both of its exclusivity mechanisms. Not refuted.

</details>

### C13. [MAJOR] Phase C insertion validity guard is not transcribed into the PG campaign, despite the spec relying on it to contain the elimination family

**Section:** §4 insertion guards

**Claim:** The design spec keeps the elimination family in-scope because it 'remains under the existing validity guards', and Phase C's guard is concrete: 'non-draw rollouts ≥ 50% of the batch AND mean game_length ≥ 6 AND obs_drama not nan'. The draft's §4 lists only RUSH/TILT/REACH-v3 and never transcribes the validity guard or its PG-era replacement (which rollout set, what replaces the drama-nan clause). Without it, the documented failure mode — 20/20 elimination genomes rollout-degenerate, 19 with mean length < 6 — flows into archives as floored-0 first-occupancy elites, polluting coverage/QD reporting and cell occupancy.

**Evidence:** docs/superpowers/specs/2026-06-12-rc2-campaign-design.md:36-38 ('Elimination family remains under the existing validity guards (known rollout-degenerate as generated — Phase C finding)'); rc2_archive/PREREGISTRATION.md:47-48 (guard definition); rc2_archive/RESULTS.md:46-51 ('All 20 elimination genomes evaluated in Stage 0 were invalid — 19 ended with mean game_length < 6').

**Proposed fix:** Transcribe the validity guard for the PG era: non-draw T1 games ≥ 50%, mean game_length ≥ 6 (from the descriptor rollouts or T1 games — pick and pin one), PG not-nan; state it is veto-on-entry, budget-consuming, counted by reason, exactly as Phase C.

<details><summary>Refuter 1 (failed to refute)</summary>

NOT REFUTED — finding is real, evidence verified, and major severity stands. (1) All three citations check out near-verbatim: the spec (docs/superpowers/specs/2026-06-12-rc2-campaign-design.md, lines 34-36 and 38) twice relies on Phase C "validity guards" (elimination in-scope commitment + "Archive: Phase C verbatim"); the guard is concretely defined at rc2_archive/PREREGISTRATION.md:47-48; the failure mode (20/20 invalid, 19 mean length <6, 1 draw-majority) is at rc2_archive/RESULTS.md:46-51 and replicated in R2 (line 121, "again 0-valid"). (2) The draft (experiments/rc2_campaign/PREREGISTRATION_DRAFT.md) §4 is a closed guard enumeration — RUSH/TILT/REACH-v3 only; grep confirms the validity guard appears nowhere, and the campaign directory holds only the draft. (3) The gap is material beyond editorial: BAR W-PG (§6) operates on "valid evaluated genomes", a term defined ONLY by the untranscribed guard, and BAR W-PG is binding with ARCHIVE_KILL consequence; the guard's obs_drama-nan clause cannot be inherited verbatim since drama no longer exists, so a PG-era replacement decision is genuinely undefined — incorporation-by-reference cannot rescue it; under house style (§10 "bars transcribed as constants", Phase C's line-by-line transcription audit) an untranscribed guard does not get built. (4) Strongest refutation attempt fails: RUSH (≥25% of DECISIVE rollouts ≤6 plies) would likely veto the 19 short-decisive elimination genomes, but it cannot substitute for the 50%-non-draw clause — draw-majority genomes have few/no decisive rollouts, were the LARGEST Phase C invalidity class (42/160 = 26% of evaluated genomes, all families), REACH-v3 covers threshold only, and the draft itself admits "draw-prone non-threshold genomes have no draw guard". These flow in as floored-0 first-occupancy elites, polluting coverage/cell occupancy and the BAR W "valid" pool. (5) The draft's DRAFT-marks section soliciting attack on adjacent issues is not a handling — this finding is precisely the graft the draft requests. (6) The proposed fix mirrors Phase C semantics exactly and breaks nothing recorded (compatible with REACH-v3; the rollout-source pinning is a decision the draft must make regardless). Caveats that do not lower severity: the QD pollution claim is mildly overstated (floored-0 elites add 0 to the QD-score sum; the real pollution is coverage, occupancy, and the BAR W denominator), and RUSH incidentally contains the decisive-short subcase — but a binding bar with an undefined input term plus an untranscribed spec commitment in a pre-lock registration is squarely major.

</details>

<details><summary>Refuter 2 (failed to refute)</summary>

FINDING CONFIRMED — NOT REFUTED. All three evidence citations verify on disk, and every refutation angle I attempted failed.

EVIDENCE CHECK (all pass, one trivial line-number slip):
1. /Users/jamesbrowne/aigame/docs/superpowers/specs/2026-06-12-rc2-campaign-design.md — the quoted sentence "Elimination family remains under the existing validity guards (known rollout-degenerate as generated — Phase C finding)" is verbatim at lines 34-36 (reviewer cited 36-38; off by two, immaterial). The spec's Architecture section additionally lists "validity guards" inside "Archive: Phase C verbatim — ..." (lines 37-39), so the design authority requires the guard twice.
2. /Users/jamesbrowne/aigame/experiments/rc2_archive/PREREGISTRATION.md:47-48 — guard defined exactly as quoted: "Insertion validity guard: non-draw rollouts ≥ 50% of the batch AND mean game_length ≥ 6 AND obs_drama not nan; invalid candidates consume budget and are counted by reason."
3. /Users/jamesbrowne/aigame/experiments/rc2_archive/RESULTS.md:46-51 — verbatim: all 20 elimination genomes invalid, 19 with mean game_length < 6; RESULTS.md:120 confirms it replicated in R2 ("elimination was again 0-valid").

REFUTATION ATTEMPT 1 — "the draft handles it elsewhere": FAILS. Grep of /Users/jamesbrowne/aigame/experiments/rc2_campaign/PREREGISTRATION_DRAFT.md for valid/guard/draw/game_length/nan shows no validity guard anywhere. §2's "(Phase C verbatim)" parenthetical scopes only to GameConfig/EvolutionConfig; §1's lineage line is descriptive. Decisive internal proof the guard was dropped rather than incorporated by reference: the draft's own DRAFT-marks (lines 140-141) say "draw-prone non-threshold genomes have no draw guard" — a statement that would be FALSE if the Phase C validity guard (non-draw ≥ 50%, all families) were inherited. The drafter's own text concedes the gap.

REFUTATION ATTEMPT 2 — "RUSH/TILT already contain the elimination failure mode": PARTIAL AT BEST, does not refute. RUSH (≥25% of decisive tactical rollouts ≤ 6 plies) plausibly vetoes the 19 too-short elimination genomes, but (a) that overlap is an unregistered inference on a different rollout record, (b) the guard's draw-majority arm is wholly uncovered for non-threshold families — Phase C Stage 0 counted 42 draw-majority invalid genomes of 160 evaluated (26%, RESULTS.md:52-54), and REACH-v3 is explicitly threshold-only, and (c) draw-majority genomes shrink the "decisive rollouts" denominator, degrading RUSH/TILT estimates themselves. RESULTS.md:49 also confirms quick_reject does NOT catch this ("structural checks pass; play is trivial"), so §2's "strict quick_reject everywhere" provides no cover.

REFUTATION ATTEMPT 3 — "over-severitized": FAILS; if anything the reviewer under-states. Beyond reported-only coverage/QD pollution, two BINDING paths are hit: (i) BAR W-PG (§6, draft line 72-73) is defined over "families with ≥ 20 valid evaluated genomes" — the word "valid" is load-bearing in a binding bar yet undefined anywhere in the draft because the validity-guard definition was dropped; with no guard, elimination could become a "sampled family" of ~zero-spread degenerate genomes and spuriously trigger ARCHIVE_KILL. (ii) §3 explicitly lets 0-floored genomes take first occupancy, and arm M selects parents by uniform-random filled cell, so degenerate first-occupants become mutation parents and drain the budget that binding BAR H-PG measures. Major is the correct severity.

REFUTATION ATTEMPT 4 — "the fix breaks something recorded": FAILS. The proposed transcription is the Phase C guard with its only literally-impossible clause (obs_drama not nan — drama is not computed in this campaign) replaced by PG not-nan, plus a pinned rollout-set choice resolving a genuine PG-era ambiguity (descriptor rollouts vs T1 games). It is consistent with §2's budget-consuming EVAL_TIMEOUT/EVAL_ERROR accounting, §4's counted-by-guard logging, the R2 prereg (PREREGISTRATION_R2.md:16 carries the guard), and restores compliance with the design spec. Nothing recorded breaks.

Verdict: finding is real, evidence-backed, not handled elsewhere, and material at MAJOR severity (binding-bar term undefined + binding-arm contamination + spec non-compliance in a document whose whole purpose is pre-data lock).

</details>

### C14. [MINOR] Heritability trigger inputs unspecified (raw vs floored PG) — the SEARCH_NEUTRAL escape hatch can fire or fail for reasons unrelated to PG heritability

**Section:** §6 slate trigger / §8 reported

**Claim:** [2 lenses converged: methodology + degeneracy's squatter finding flagged the same r-distortion] The SEARCH_NEUTRAL escape hatch (parent-child T1-PG r ≥ 0.3 → 2× replicate) doesn't say whether r is computed on raw or floored PG. Floored: a large (0,0) point mass — parents/children that floor to 0 dominate the pairs (3/7 anchors floor at T1) — distorts Pearson r in either direction. Raw: imports the registered-as-noise negative-region orderings ('misalignment, not relative depth'). Phase C's validated convention was raw values ('parent pooled drama at selection, child first-batch drama'), and the 0.3 threshold was calibrated on drama's r (0.344/0.444 observed) and is being reused for a different signal — acceptable as an owner choice, but the input values must be pinned for the trigger to be auditable.

**Evidence:** PREREGISTRATION_DRAFT.md:81-84; rc2_archive/PREREGISTRATION.md:88-90 (per-step log convention); rc2_archive/RESULTS.md:37-39, :113-114 (r = 0.344/279, 0.444/533 on raw drama, no flooring); cost_tiering.json T1: S1 −0.0208, S4 −0.4167, e1453 −0.125 (3/7 floor to 0).

**Proposed fix:** Pin: Pearson r on RAW T1-PG (parent pooled-at-selection vs child first-batch), restricted to pairs whose parent has floored PG > 0; pairs with EVAL_TIMEOUT/EVAL_ERROR children excluded and counted; report floored-r and the excluded-pair count as diagnostics alongside.

<details><summary>Refuter 1 (failed to refute)</summary>

Finding verified end-to-end and survives. (1) The draft's trigger (PREREGISTRATION_DRAFT.md:81-84) says "parent-child T1-PG heritability r from the M-log" without pinning raw vs floored, and never defines the M-log fields — a regression from Phase C, whose prereg pinned them explicitly (rc2_archive/PREREGISTRATION.md:88-90: parent pooled drama at selection, child first-batch drama) and whose runner (run_probe.py:562-572) computed np.corrcoef on those raw values. (2) The ambiguity is genuine, not pedantic: the draft's naming convention ("floored" written explicitly wherever flooring applies) suggests raw, but the inherited "Phase C verbatim" log convention records quality-at-selection, which in this campaign IS the floored value — the two conventions point in opposite directions, so either implementation could claim conformance. (3) Numbers check: cost_tiering.json T1 gives S1 −0.0208, S4 −0.4167, e1453 −0.125 — exactly 3/7 anchors floor to 0; r = 0.344/279 and 0.444/533 match RESULTS.md:37-39/:113-114; the negative-region "misalignment, not relative depth" rule is registered at ANCHOR_CALIBRATION.md:43-45, so raw r really imports registered-as-noise orderings. (4) Not handled elsewhere: the design spec (2026-06-12-rc2-campaign-design.md:68-70) says only "parent-child PG r ≥ 0.3", and the draft's DRAFT-marks attack list omits this. (5) Material at the claimed severity: Phase C fired this exact trigger at r=0.344 vs 0.3 (0.044 margin) and that firing led to the ARCHIVE_GO replicate; a (0,0) point mass over hundreds of pairs (0-PG cell squatters are acknowledged in the draft itself) can plausibly move r by more than that margin, so the estimator choice can flip a registered branch. (6) The fix breaks nothing recorded — rc2_campaign/ holds only the draft, and the pin would land pre-lock/pre-data per §10. Two non-load-bearing nits: "calibrated on drama's r" is loose (0.3 was registered pre-data in Phase C, only validated on drama's observed r — the finding concedes the reuse as owner choice anyway), and the proposed fix still admits child-side negative-region noise via raw child values; neither undermines the core requirement that the trigger input be pinned. Severity "minor" is correctly calibrated: escape-hatch branch, not the binding GO/NO-GO path, but auditable-spend-deciding.

</details>

<details><summary>Refuter 2 (failed to refute)</summary>

Finding survives. Every evidence citation verified on disk: (1) PREREGISTRATION_DRAFT.md:81-84 trigger says "parent-child T1-PG heritability r from the M-log" with no raw/floored designation, and the M-log contents are never defined in the draft (Phase C's prereg pinned the tuple; this one doesn't); (2) rc2_archive/PREREGISTRATION.md:88-90 log convention confirmed verbatim; (3) r=0.344/279 and 0.444/533 confirmed at RESULTS.md:37-39/113-114, and run_probe.py:562-565 confirms Pearson on raw parent_pooled_drama vs child_batch_drama with no flooring anywhere in Phase C; (4) cost_tiering.json T1 values match exactly — S1 −0.0208, S4 −0.4167, e1453 −0.125, i.e. 3/7 anchors floor to 0; (5) the "misalignment, not relative depth" registration confirmed at rc2_planning_gap/ANCHOR_CALIBRATION.md:43-45. My strongest refutation attempt — that the draft's term grammar already pins raw (§3 defines T1-PG as the raw score; "floored" is always explicitly marked, e.g. BAR H-PG) — fails because it collides with the equally-registered "Phase C machinery verbatim" inheritance: the M-log's Phase C field is the parent's quality-at-selection, and RC2 quality is the floored transform. In Phase C raw and quality were numerically identical (drama ≥ 0), so the convention never distinguished them; RC2 is the first configuration where they diverge, leaving two internally consistent readings that yield different r values against a binding 0.3 threshold. The floored-reading point mass is real (floored-0 genomes enter via first-occupancy per §3 and are selected uniformly as parents). The draft does not resolve this elsewhere (§8 lists r as reported, but §6 makes it binding on the SEARCH_NEUTRAL branch; the DRAFT-marks attack list omits it; the design spec is equally unqualified). The proposed fix breaks nothing recorded — it matches the Phase C raw convention; the EVAL_TIMEOUT/EVAL_ERROR exclusion is largely redundant (failed evals produce no PG value, so no pair forms) but harmless as a counted diagnostic. Severity "minor" is correctly calibrated: a binding branch input that must be pinned pre-lock per §10's audit obligations, but owner-gated, single-branch, one-sentence fix. Residual nits that do not overturn: "calibrated on drama's r" is loose wording (0.3 was pre-registered before 0.344 was observed — drama r values are its only empirical track record, not its calibration source), and the parent-PG>0 pair restriction slightly decouples the statistic from the 0.344/0.444 all-pairs provenance, a trade-off the finding already discloses via its "owner choice" concession and floored-r side report.

</details>

### C15. [MINOR] Blind-pack convention mismatch: stage3_ab is a 2-team × 3-label pack; the campaign needs 3 teams × 7 labels — the closer registered precedent is the Phase D pack

**Section:** §7 blinding reference

**Claim:** [2 lenses converged: groundtruth + repofacts] stage3_ab's briefing, templates, and orchestrator section are hardcoded for 2 teams × 3 games = 6 verdicts with labels D/V/X, an A1-specific validity band [3.9,4.4], and board-size-specific play.py action-ID help (cell = q + 22*r; pass=484; swap=485). Citing it as the convention for a 3-team × 7-game slate under-specifies label assignment, shuffle seed, template count, .blind_mapping.json shape, and unblinding order. Phase D already built the exact needed shape (7 blind labels A–G, seed-99 shuffle, 3 teams, 21 verdicts, unblinding only after all verdicts filed).

**Evidence:** evaluations/stage3_ab/BRIEFING.md:1-9, :104-119 ('one of 2 independent evaluator teams … THREE games, labeled D, V, and X'; X-band [3.9,4.4]); template files TEMPLATE_team-N_game{D,V,X}.md only; evaluations/rc2_phase_d/PREREGISTRATION.md:31-34, :51-52 (7-label seed-shuffled pack, 3 teams); PREREGISTRATION_DRAFT.md:106-107.

**Proposed fix:** Cite the rc2_phase_d pack convention (7 labels, registered shuffle seed, verdict-file naming, unblind-after-all-filed) as the base, with the stage3_ab additions worth keeping named explicitly (fairness-perception probe, cross-game comparison section, role win-split logging); regenerate play.py help per game's actual action space.

<details><summary>Refuter 1 (failed to refute)</summary>

Finding verified on disk and survives adversarial review. (1) Evidence checks out: stage3_ab/BRIEFING.md is hardcoded for 2 teams x 3 labels D/V/X (lines 3-4), 6-verdict unblinding (lines 118-119), an FC-specific X validity band [3.9,4.4] (lines 133-135 — cited as :104-119, trivial line slop, content as described), FC-specific GO/PARTIAL thresholds, and board-specific action help 'q + 22*r; pass=484; swap=485' (line 51); only 3 TEMPLATE files exist. The draft (PREREGISTRATION_DRAFT.md:105-107) cites this as the pack convention for a 3-team x 7-game, 21-verdict slate (sections 6-7), and registered slate member S3 (0165399e5aef, moore 3D axis-4) cannot even be addressed by the stage3_ab action formula. rc2_phase_d/PREREGISTRATION.md:31-34,51-52 is the exact-shape precedent (7 labels A-G, seed-99 shuffle, 3 teams, 21 verdicts, unblind-after-all-filed). (2) The draft does NOT handle the gap elsewhere: label space, shuffle seed, templates, and unblind-count are specified nowhere except via the wrong-shaped citation; the shuffle seed is a real open registered degree of freedom that Phase D pinned. (3) The fix breaks nothing recorded: the design spec uses the same stage3_ab wording but citation choice is not a recorded owner decision, and the draft explicitly awaits panel grafts pre-lock. The fix is actually simpler than proposed — Phase D's BRIEFING already contains all three 'stage3_ab additions worth keeping' (fairness probe line 99, cross-game comparison line 120, role win-split logging line 155) plus generalized per-game --legal action decode, so citing rc2_phase_d as the base loses nothing. (4) Severity 'minor' is correct: no corruption of bars or decision grammar (draft registers its own band and counts), but a genuine under-specification in a document meant to lock. Not refutable on existence, materiality, evidence, severity, or fix safety.

</details>

<details><summary>Refuter 2 (failed to refute)</summary>

Finding survives. Every cited evidence item checks out on disk, and my attempted refutations failed.

Evidence verification (all confirmed):
1. /Users/jamesbrowne/aigame/experiments/rc2_campaign/PREREGISTRATION_DRAFT.md lines 105-107: §7 says "Blind-packed per evaluations/stage3_ab convention" — and grep confirms this is the ONLY blinding-procedure reference in the draft; rc2_phase_d is never cited (the lineage header cites Phase C machinery, planning-gap, and guards only).
2. /Users/jamesbrowne/aigame/evaluations/stage3_ab/BRIEFING.md is hardcoded exactly as claimed: line 3-4 "one of 2 independent evaluator teams ... THREE games, labeled D, V, and X"; line 51 board-specific action help "cell index = q + 22*r; pass=484; swap=485" (22-wide board, 484=22*22); lines 117-118 "all 6 verdicts (2 teams × 3 games)"; lines 134-135 A1 validity band [3.9, 4.4] on label X. Templates on disk are only TEMPLATE_team-N_game{D,V,X}.md. Its .blind_mapping.json is a flat 3-key map ({"D": "m_siege", "V": "s_flip_r2", "X": "a1_field_connect"}) with NO registered shuffle seed — labels carry fixed semantics in the orchestrator decision section, so the stage3_ab convention genuinely contains no label-randomization element to inherit.
3. The campaign does need 3 teams × 7 labels: draft §6 registers "3 independent blind tmux teams, 21 verdicts" and §7's slate is top-5 M-elites + d4015 + S3 = 7 games; 3×7=21, internally consistent and matching the claim.
4. /Users/jamesbrowne/aigame/evaluations/rc2_phase_d/PREREGISTRATION.md lines 31-32 and 51-52 confirm the Phase D pack is the exact needed shape: 7 blind labels A-G, seed-99 shuffle registered (label_seed: 99 is literally in its .blind_mapping.json), 3 teams, team-{N}_game{A..G}.md naming, unblind only after all 21 filed. Its BRIEFING also solves the action-ID problem generically ("Action-id schemes differ per game ... read the scheme from --rules and decode with --legal"), which a heterogeneous QD-archive slate requires and which stage3_ab's q+22*r help actively contradicts.

Refutation attempts that failed:
- "Draft handles it elsewhere": No. §6 registers team/verdict counts and its own d4015 band [3.48, 4.18] (so the band specifically is NOT at risk of inheritance — the finding correctly cites the X-band only as evidence of stage3_ab's A1-specificity), but shuffle seed, label set, verdict-file naming, template convention, and per-game action decode appear nowhere in the draft.
- "Fix breaks something recorded": The design authority doc (docs/superpowers/specs/2026-06-12-rc2-campaign-design.md lines 25, 100) also says "per the stage3_ab convention" / "tooling reused from stage3_ab" — so switching the prereg citation nominally diverges from that recorded wording. But the draft explicitly invites panel grafts pre-lock, and the owner decision's substance (3 blind tmux teams, ~7 games, no provenance) is preserved and better operationalized by the phase_d convention. This confirms the gap is systemic rather than refuting it.
- "Convention citation generalizes": Partially — unblind-after-all-verdicts IS part of the stage3_ab text (lines 13-14), so "unblinding order" in the claim is mildly overstated; but the load-bearing missing elements (registered shuffle seed, 7-label assignment, mapping shape, templates, action-space help) cannot be supplied by a pack that lacks them.

Minor quibble with the proposed fix (does not rescue a refutation): the three "stage3_ab additions worth keeping" (fairness probe, cross-game comparison, role win-split logging) are ALREADY all present in rc2_phase_d/BRIEFING.md (lines 99-107, 120-128, 155-161), so the fix is even simpler than stated — cite phase_d alone.

Severity: minor is correct. No bar or decision grammar is wrong; §6's counts already match the Phase D shape; this is a one-line citation fix plus pack-build specification. But it is material at that level: the draft locks pre-data, and the blinding citation as written leaves label assignment and shuffle as unregistered researcher degrees of freedom at pack-build time.

</details>

## Refuted (did not survive verification)

### R1. [blocker] 8h cap is arithmetically incompatible with B=600/arm plus the full-conv re-eval ladder; CAL-C's projection omits the dominant cost lines and the re-scope path re-enters the known-failure B regime

**Claim:** [2 lenses converged: methodology (blocker) + repofacts] Recorded T1 cost: 2210.9 CPU-s for 7 genomes = 315.8 CPU-s/genome mean (range 131–543). Screening 1200 arm-genomes + ~160 Stage-0 ≈ 119 CPU-h; elite re-evals at full convention (~20 CPU-min/genome) × ~390 re-eval calls (Phase C R2's recorded count at B=600, 3 checkpoints; R2 archives 42+31 cells) ≈ 130 CPU-h; total ≈ 250 CPU-h ≈ 35.6 wall-h on 7 workers — 4.5× the 8h cap, EXCLUDING tactical guard rollouts and challenger top-up batches. CAL-C times only '20 fresh genomes through the full per-genome pipeline' — covering T1 screening but not

**Why refuted:** REFUTED as a blocker. The cited line numbers and figures all check out (cost_tiering.json:157-165 cpu_s_total 2210.9, range 130.9–543.0; COST_TIERING.md:31-33; RESULTS.md:21/61-64/104/113/115; spec line ~100 "1.5–3 hr wall (cap 8 hr, 7 workers)"; draft lines 65-68 and 97), and the reviewer's internal arithmetic is correct (1360×315.8 CPU-s ≈ 119 CPU-h; 390×20 CPU-min ≈ 130 CPU-h; ≈35.6 wall-h on 7 | REFUTED at claimed blocker severity. The finding's raw citations transcribe correctly (cost_tiering.json T1 = 2210.9 CPU-s/7 genomes = 315.8 CPU-s/genome mean, range 130.9–543.0; COST_TIERING.md:31-33 full-conv ~20 CPU-min/genome; rc2_archive/RESULTS.md:21 run-1 BAR H 0.0194 FAIL at B=300, :104 R2 0.0574 PASS at B=600, :112-115 archives 42/31 and 390 elite re-evals; spec :100-101 "1.5–3 hr wall (c

### R2. [blocker] 180s per-eval timeout (Phase C verbatim) cannot run the campaign's own anchors, CAL-I, or re-evals — and acts as an endogenous-cost selection bias against deep games

**Claim:** [2 lenses converged: degeneracy (blocker) + repofacts] The 180s per-eval wall timeout was calibrated for Phase C's sub-second random/greedy rollout batches. A campaign 'eval' includes a T1 screening of 24 UCT games, measured at 131–543 CPU-s per genome: 6 of 7 anchors exceed 180s, including BOTH CAL-I instruments (d4015 280.5s, S4 448.5s) and S1/S3/e1453. Full-convention re-evals (256v16, n=48) cost ≈4× T1 (~20 CPU-min on anchor-class games). As drafted, CAL-I times out on its own anchors → spurious PROBE_INVALID, and elite re-evals/slate-anchor evals trip EVAL_TIMEOUT, consuming budget. The d

**Why refuted:** REFUTED at claimed severity. The cited numbers are real (verified in /Users/jamesbrowne/aigame/experiments/rc2_planning_gap/cost_tiering.json and COST_TIERING.md) but the inference chain fails at four load-bearing points.

(1) CPU-s vs wall-s conflation. The draft's timeout (PREREGISTRATION_DRAFT.md:32) is explicitly a "per-eval WALL timeout 180 s"; the cited 131–543 figures are CPU-seconds. The c | REFUTED at claimed severity. The citations are accurate (cost_tiering.json T1 CPU-s per genome verified exactly; Phase C inheritance at rc2_archive/PREREGISTRATION.md:31-33 and run_probe.py EVAL_TIMEOUT_S=180 verified; COST_TIERING.md and RESULTS.md:61-64 quoted correctly), but every load-bearing inference fails verification.

(1) CPU-vs-wall conflation. The headline "6 of 7 anchors exceed 180s" t

### R3. [major] Guard-invisible degenerate shape: timeout-decided draw-free packing races slip every guard because REACH-v3 is scoped threshold-only

**Claim:** The anchor addendum's registered warning is that PG search 'could in principle favor games whose dynamics make random rollouts informative (model-alignment) rather than deep'. The concrete genome that wins this way: a territory (or any non-threshold) game decided at max_turns by score count. Every game is decisive (zero draws → REACH draw-share would never fire even if it applied), games are long (RUSH silent), seat-balanced T1 self-cancels tilt and TacticalAgent play is symmetric (TILT silent), and REACH-v3 is threshold-family-only so it is never even computed. Deep UCT reliably out-packs sha

**Why refuted:** REFUTED at claimed severity. The finding's mechanism is contradicted by the on-file record at three load-bearing points, its fix's claimed validation evidence is a misreading of the cited file, and the residual kernel is already disclosed in the draft with a binding backstop.

(1) "TILT silent" on packing races is false on the record. /Users/jamesbrowne/aigame/experiments/rc2_descriptor_v2/RESULTS | REFUTED at claimed severity (major); residue is a minor graft on an already-registered panel mark. (1) The finding's load-bearing step — packing races yield "high floored PG → elite → slate seat" — is contradicted by the project's own recorded measurements of exactly that genome class: territory parity-race anchors S4/S5 have PG −0.323 and +0.052 (experiments/rc2_planning_gap/ANCHOR_CALIBRATION.md

### R4. [major] S-GO-2 carry-in containment unspecified: anchors can enter the binding pools, and no canonical-hash dedup stops an elite duplicating d4015/S3 in the blind pack

**Claim:** [2 lenses converged: degeneracy + groundtruth; methodology's S-GO-2 finding flagged the same ambiguity] Two containment holes around the d4015/S3 carry-ins. (1) 'Top-2-by-full-PG slate elites' is not explicitly anchor-exclusive: S3 (full PG +0.479, within 0.021 of the metric ceiling) and d4015 (+0.438) will plausibly outrank most or all campaign elites, so the bar can be corrupted in BOTH directions — d4015 in the top-2 imports a known ~3.83 game and can manufacture a +0.5 separation over two degenerate bottom elites (spurious PASS proving only that PG detects its calibration anchor); S3 in th

**Why refuted:** REFUTED AT CLAIMED SEVERITY. Every citation checks out on disk (S3 full PG +0.479 / d4015 +0.4375 in /Users/jamesbrowne/aigame/experiments/rc2_planning_gap/cost_tiering.json; blind 3.1 / 3.83 in BLIND_SEVEN_VALIDATION.md; per-arm-only dedup at rc2_archive/PREREGISTRATION.md:56-57; the canonical-hash precedent at rc2_descriptor_v2/probe_results.md), and the draft indeed lacks an explicit pool-exclu | REFUTED at claimed severity (major). The cited evidence is accurate (verified: S3 full PG +0.479 and d4015 +0.4375 in /Users/jamesbrowne/aigame/experiments/rc2_planning_gap/cost_tiering.json full_reference; blind means 3.1/3.83 in BLIND_SEVEN_VALIDATION.md; the hash-check precedent at rc2_descriptor_v2/probe_results.md:9; per-arm dedup scope at rc2_archive/PREREGISTRATION.md:56-57; draft lines 88-

### R5. [major] BAR H-PG estimator is biased and mis-derived: floor σ taken from the wrong instrument, and arm-asymmetric winner's-curse insertion luck persists in pooled values at the same order as the floor

**Claim:** [2 lenses converged: methodology + degeneracy] BAR H is computed on full-convention (256v16, n=48) post-re-eval values, but the draft derives the 0.05 floor and σ≈0.032 from T1 noise. Recomputed from the only full-convention replicate data (per-stream pairs across anchor_calibration + blind_seven): σ(full, n=48) = 0.053 → σ(top-10-mean difference) = 0.024, so 0.05 ≈ 2.1σ — coincidentally more conservative than the stated 1.5σ, but the derivation is wrong, and σ depends on each elite's pooled n (re-evals at 3 checkpoints can take n to 144), never pinned. Worse, insertion is a selection event on

**Why refuted:** REFUTED at claimed severity (major); residual kernel is minor (two clarification grafts). The claim's arithmetic all reproduces: draft line 134-135 does derive the BAR H floor note from T1 noise (0.07/sqrt(10)*sqrt(2)=0.031) while BAR H runs on full-conv n=48 values; recomputed from the 7 per-stream pairs in ANCHOR_CALIBRATION.md + BLIND_SEVEN_VALIDATION.md, sigma48=0.0532, sigma_top10diff=0.0238, | FINDING SURVIVES at major. I attempted refutation on every leg and the evidence held; I independently reproduced every number the reviewer cites.

VERIFIED ARITHMETIC. (1) Wrong-instrument floor derivation: /Users/jamesbrowne/aigame/experiments/rc2_campaign/PREREGISTRATION_DRAFT.md:76-79 defines BAR H-PG on full-convention (256v16, n=48) post-re-eval values, while lines 134-135 derive "σ ≈ 0.032 u

### R6. [major] d4015 validity band [3.48, 4.18] has no variance provenance: 11–26% spurious CAMPAIGN_UNRESOLVED (59% under r8-spread), anchor-drift re-centering, a one-sided harsh-drift hole, and a non-terminating replicate branch

**Claim:** [3 lenses converged: methodology + groundtruth + repofacts] The band centers on a single 3-verdict campaign reading (Phase D 3.83 = 4.1/4.0/3.4, SD 0.38) and discards the same game's other recorded blind reading (r8_replay 4.10 over 5 teams, σ 1.14 — Phase D's own registered band was [3.7, 4.5] centered on that prior, and 3.83 passed 'at the low edge'); re-centering each campaign on the latest single reading is an anchor random walk. The ±0.35 width is justified nowhere: with both means noisy, σ_diff ≈ 0.31 → ±0.35 ≈ 1.1σ → ~26% false CAMPAIGN_UNRESOLVED; center-treated-exact gives ~11% (team 

**Why refuted:** NOT REFUTED — the core finding is real, evidence-verified, and major; but three framing sub-claims are wrong and should be corrected before the graft is applied.

VERIFIED: (1) PREREGISTRATION_DRAFT.md:94-96 registers [3.48, 4.18] = 3.83 ± 0.35 with no variance derivation, while every sibling binding constant has one (CAL-I 3×σ_diff at :62-64, BAR W-PG 3×σ at :73-74, BAR H-PG σ≈0.032 at :135); :13 | VERDICT: REFUTED at claimed severity. The arithmetic is correct but the finding's load-bearing evidentiary characterizations fail verification, its headline scenario uses an inapplicable instrument class, its scariest structural claim (unbounded loop) is contradicted by the cited text and its graft lineage, and the surviving kernel is precisely the derivation work the DRAFT's own panel-solicitatio

### R7. [major] S-GO-1 'blind team mean' is ambiguous (one reading already passed by plateau controls), and as max-over-5 on 3-verdict means it false-passes ~40% under the plateau null with no near-bar replicate path

**Claim:** [2 lenses converged: degeneracy + groundtruth (two findings); methodology's grammar finding flagged the same ambiguity] '≥ 1 M-elite blind team mean ≥ 4.10' has two readings: (a) the game's mean over the 3 teams' Overall verdicts, or (b) at least one team's score ≥ 4.10. The project's own vocabulary pulls toward (b) — run21's table row 'team mean (calibration)' is a PER-TEAM mean — and under (b) the bar is near-vacuous: Phase D's plateau control e1453 drew a 4.3 from one team (3-team mean only 3.90) and d4015 drew a 4.1 (mean 3.83). Even under reading (a) the bar is a max-over-5 selection on 3

**Why refuted:** REFUTED at claimed severity (major). The evidence citations are accurate (I verified every number: run21 per-game SDs 0.27-0.36 and team means 3.84/3.53/3.99/3.53/3.54 at evaluations/run21/SUMMARY.md:11-20; Phase D rows e1453 3.6/4.3/3.8->3.90 and d4015 4.1/4.0/3.4->3.83 with per-game SDs 0.23-0.78 at evaluations/rc2_phase_d/RESULTS.md:20-26; R8 4.10/sigma 1.14; FC 4.15 vs 3.45; design spec :19-20 | REFUTED at claimed severity (major). The citations are accurate (verified: PREREGISTRATION_DRAFT.md:85-87; run21/SUMMARY.md:20 team means 3.84/3.53/3.99/3.53/3.54, per-game SDs 0.27-0.36; rc2_phase_d/RESULTS.md:20-26 e1453 3.6/4.3/3.8->3.90, d4015 4.1/4.0/3.4->3.83, SDs to 0.78; r8_replay 4.10/sigma 1.14; FC probe 4.15 vs 3.45; design spec line 19), but the finding's three inferences each fail.

(

### R8. [major] CAL-I drops the S5 leg — the closeness confound the gate was registered to test; the S4-only check has ~5σ slack and only detects total instrument death

**Claim:** [2 lenses converged: methodology + repofacts] The registered binding validation was demonstration on the 'S4/S5 vs d4015' pair — d4015 strictly above BOTH (anchor_calibration.py's pair gate includes S5) — where S5 (close-but-shallow, +0.052) is the actual closeness-confound control and S4 (−0.323, a 2/22-loss game) is the easy far control. The draft's CAL-I tests only PG(d4015) − PG(S4) ≥ 0.30: T1 expected margin 0.834 leaves ~0.53 (≈5σ_diff) of slack, so it fails only on gross instrument breakage and is nearly unfailable otherwise, while the leg that does the confound work (T1 expected margin

**Why refuted:** Evidence verified on disk: the claim's file citations and numbers are accurate (pair gate includes S5 in anchor_calibration.py:42-44 and ANCHOR_CALIBRATION.md:16; CAL-I in PREREGISTRATION_DRAFT.md:61-64 tests only d4015−S4 ≥ 0.30 and S5 appears nowhere in the draft; T1 margins 0.834 and 0.375 reproduce from cost_tiering.json; the 0.087 pooled per-stream σ reproduces). However, the finding fails at | REFUTED at claimed severity. The citations are accurate (I verified all of them: CAL-I at PREREGISTRATION_DRAFT.md:61-64 tests only PG(d4015)−PG(S4) ≥ 0.30; the registered gate in experiments/rc2_planning_gap/anchor_calibration.py:42-44/189-194 is PG(d4015) > PG(S4) AND > PG(S5); cost_tiering.json T1 values are d4015 +0.4167, S5 +0.0417, S4 −0.4167, so the 0.834 and 0.375 margins check out), but t

### R9. [major] 0-floored squatters poison M-arm parent selection: ~45% of true-zero genomes insert, never-evict makes noise cells permanent parents, diluting M's budget and biasing BAR H toward SEARCH_NEUTRAL

**Claim:** [2 lenses converged: methodology + degeneracy; the draft's own panel mark names the squatting but not the budget drain] Quality = max(PG,0) with first-occupancy, '0-vs-0 never displaces', and 're-eval never evicts' means cells claimed by deeply-negative genomes are permanent until a strictly-positive challenger arrives. Quantified: P(T1 measures > 0 | true PG ≤ 0) ≈ 0.45 at σ=0.087, and roughly half the generator space is true-PG≤0 (3 of 7 anchors floor to 0 at T1: S1 −0.021, S4 −0.417, e1453 −0.125; 4 of 7 full-conv ≤ 0.05) — so a large share of first-occupancy elites are noise. A cell whose 

**Why refuted:** REFUTED at claimed severity (major). Evidence citations are mechanically accurate (all line numbers and values in /Users/jamesbrowne/aigame/experiments/rc2_campaign/PREREGISTRATION_DRAFT.md, /Users/jamesbrowne/aigame/experiments/rc2_planning_gap/cost_tiering.json, /Users/jamesbrowne/aigame/experiments/rc2_archive/PREREGISTRATION.md and RESULTS.md verified; sigma=0.087 reproduces as 0.0866 from the | REFUTED at claimed severity (major); the underlying squatter mechanism is real but is a minor reporting graft, not a validity flaw. Verification (all files re-read): citations check out (PREREGISTRATION_DRAFT.md lines 24-26, 41-44, 136-141; rc2_archive/PREREGISTRATION.md:53-55, 87-88; RESULTS.md:40-42), sigma=0.087 reproduces from cost_tiering T1 per-stream pairs (recomputed 0.0866), and P(measure

### R10. [major] Determinism rules insufficient for a bit-identical rerun under 7-worker parallelism

**Claim:** Phase C's bit-identical-rerun guarantee (cited by the draft as the inherited audit pattern) was demonstrated on a single-process 10–25-minute run. The campaign runs hours on 7 workers, and the M arm's step t depends on archive state after step t−1: with asynchronous evaluation/insertion, archive contents at each cell-selection draw depend on worker completion order, so a rerun is not bit-identical even with content-derived eval seeds. Also unpinned: how the two T1 streams are derived from eval_seed (the draft says 'derived from the eval seed' without the rule), the batch_index counting rule (P

**Why refuted:** REFUTED at claimed severity. The citations check out (spec line 101 does say "cap 8 hr, 7 workers"; RESULTS.md:65-68/:78-79 and rc2_archive/PREREGISTRATION.md:22-24 read as quoted; Phase C's run_probe.py has zero parallelism constructs, so the bit-identical rerun was indeed demonstrated single-process at 10.5/24.7 min). But the finding's central mechanism and three of its four sub-claims fail on i | REFUTED at claimed severity. The line citations all check out (spec:101 does say 7 workers/8h cap; RESULTS.md:65-68/78-81 do describe a bit-identical relaunch of a 10.5-min single-runner probe with a determinism review; rc2_archive/PREREGISTRATION.md:22-24 does contain the batch_index counting rule), but the finding's central inference fails on four independent grounds. (1) Misattributed guarantee

### R11. [minor] 'Phase C checkpoints ×2' mislabels the validated lineage: the actual B=600 replicate re-evaluated every 100 evals (6 checkpoints), not 3

**Claim:** The draft pins re-evals 'at eval counts 200/400/600 per arm (Phase C checkpoints ×2)'. The validated B=600 run (R2, the same 'validated replicate scale' the draft's B leans on) used full-archive re-evals after every 100 evals — 100/200/300/400/500/600 — and its noise-machinery validation ('M's lead survives 6 full-archive re-evals') is at that cadence. Halving cadence is a defensible cost choice given full-conv pricing, but the lineage label is wrong and the deviation is unstated.

**Why refuted:** REFUTED. The reviewer's factual citations check out, but the finding's two core claims — "the lineage label is wrong" and "the deviation is unstated" — both fail on re-reading.

Verified facts: (1) Draft §3 (PREREGISTRATION_DRAFT.md:45-47) says re-evals "at eval counts 200/400/600 per arm (Phase C checkpoints ×2)". (2) R2 (experiments/rc2_archive/PREREGISTRATION_R2.md:13-14) used "full-archive re- | REFUTED. All three evidence citations check out verbatim (PREREGISTRATION_DRAFT.md:45-47; PREREGISTRATION_R2.md:13-14; RESULTS.md:115-117), but the finding's core assertion — that '(Phase C checkpoints ×2)' mislabels the lineage — is wrong on verification. The canonical Phase C protocol, /Users/jamesbrowne/aigame/experiments/rc2_archive/PREREGISTRATION.md line 53, registers full-archive re-evals '
